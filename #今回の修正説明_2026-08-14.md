# ERB起動時allocation削減と解析基盤の整理

## 概要

ERB起動時の解析処理について、既存の挙動・互換性を維持したまま一時オブジェクトの生成量を減らすため、Argument ParserとLexer周辺の内部データ構造を整理した。

主な変更は、`ExpressionParser.ReduceArguments` の一時collectionのList化、`WordCollection` のCompact表現と必要時だけのLinkedListへの遅延変換、Lexer結果用Listの初期確保方法の最適化である。

また、性能調査で継続利用できる汎用profiling機能を `PERFORMANCE_METRICS` ビルド限定で整理した。

今回確認できた主な効果は **allocation削減** である。起動時間・CPU時間については実行順や環境による揺れが大きく、現時点では速度改善率を確定していない。

## 変更方針

今回の最適化では、以下を優先した。

- 既存ERBとの互換性を最優先する。
- ParserやLexerの構文解析ルール自体は変更しない。
- Macro、Pointer、挿入・削除など、既存のnode操作が必要な処理を壊さない。
- allocationを減らせても速度改善が再現しない場合は、速度向上を採用理由にしない。
- 計測専用コードは通常Releaseへ不要なoverheadを持ち込まない。
- Phase固有の一時measurementは検証終了後に撤去し、汎用Profilerだけを残す。

## ReduceArgumentsの一時collectionをList化

`ExpressionParser.ReduceArguments` は、解析した複数の `AExpression` を一時的に保持するために `LinkedList<AExpression>` を使用していた。

この処理で実際に必要だったのは、末尾への追加、末尾要素の参照、順序を保った返却であり、LinkedList固有のnode操作は使っていなかった。

そのため内部collectionを `List<AExpression>` へ変更した。

### 変更前

```csharp
var terms = new LinkedList<AExpression>();
terms.AddLast(...);
return [.. terms];
```

### 変更後

```csharp
var terms = new List<AExpression>();
terms.Add(...);
return terms;
```

これにより、解析ごとに生成されていた `LinkedList<AExpression>` と `LinkedListNode<AExpression>` のallocationを避ける。

Allocation削減は確認できたが、複数回の交互測定では起動時間・ScriptParse時間の改善は安定して再現しなかった。そのため、この変更の速度効果は **UNPROVEN** とする。

## WordCollectionのCompact表現

Lexerが生成するtoken列は従来、基本的に `LinkedList<Word>` とその `LinkedListNode<Word>` で保持していた。

調査の結果、通常の解析ではWordCollectionの大部分が単純な順次追加・順次読み取りだけで終了し、LinkedList nodeの挿入・削除機能を必要とするcollectionは非常に少なかった。

そこで通常状態を、

```text
List<Word> + index cursor
```

とするCompact表現へ変更した。

一方、Macro展開などで以下が必要になった場合だけ、

- `LinkedListNode<Word>`
- Pointerの前後参照
- Insert / InsertRange / Remove
- node identity

既存の `LinkedList<Word>` へ一方向に遅延変換する。

```text
Compact List
    ↓ node操作が必要
EnsureLinked()
    ↓
LinkedList
```

これにより、通常のWordCollectionではLinkedList nodeを生成しない。

## WordCollectionの互換性

Compact化にあたり、単純なList置換では既存挙動が変わる箇所を個別に確認した。

特に以下を維持している。

- `Count`
- `Current`
- `EOL`
- `ShiftNext`
- `PointerReset`
- `Pointer`
- `Collection`
- `Insert`
- `InsertRange`
- `Remove`
- Clone
- Macro展開
- Compact→Linked変換後のcursor
- `Add(WordCollection)` の通常追加

### self-add

旧実装の `wc.Add(wc)` は、LinkedListを列挙中に同じcollectionへ要素を追加するため、最初の追加が行われた後に `InvalidOperationException` が発生する。

Compact Listで単純に `AddRange(this)` とすると、例外を出さず全要素を複製してしまい既存挙動と異なる。

そのためself-addだけは `ReferenceEquals(this, wc)` で旧LinkedList経路へ戻し、例外型・メッセージ・途中まで発生する副作用を含めて旧挙動と一致させた。

## WordCollection Compact化のAllocation結果

同一作業ツリーからCompact化だけを除いたBeforeと、Compact化後のAfterを作成して比較した。

### 通常構成

```text
Total Allocated
2,434,169,072
→
2,369,594,408

-64,574,664 bytes
-2.65%
```

### 大量口上構成

```text
Total Allocated
6,798,623,216
→
6,484,039,664

-314,583,552 bytes
-4.63%
```

大量口上構成の sampled `LinkedListNode<Word>` は、

```text
698,366,800
→
744,408 bytes
```

となり、約 **99.89%減少** した。

代わりに `Word[]` と `List<Word>` のallocationは増えるが、それらを含むprocess total allocation自体が低下していることを確認した。

この比較値はWordCollection Compact化単独のBefore / After結果であり、他の最適化結果と単純合算して総改善量とはしない。

速度効果は引き続き **UNPROVEN** である。

## Lexer-only Lazy Capacity-8

Compact化後のfresh profileでは、`List<Word>` のgrowthに伴う `Word[]` allocationと `Array.Copy` が残っていた。

実測では大量口上構成で、

```text
4 → 8 resize
1,241,760回

resize時にコピーされたWord参照
13,032,984 references
```

が確認された。

一方、Lexer結果には0-tokenや1-tokenの小さいcollectionも多いため、全WordCollectionを最初からCapacity 8にすると不要allocationが増える。

そこでLexerが生成するWordCollectionだけを専用のPending状態で開始する。

```text
LexerPending
compactCollection == null
linkedCollection == null
```

tokenが1個も無い場合は、そのまま終了する。

```text
0 token
→ List<Word>を生成しない
→ Word[]も生成しない
```

最初のtokenが実際に追加された時点でのみ、

```csharp
new List<Word>(8)
```

を生成する。

これにより、

```text
0 → 4 → 8
```

だった経路を、

```text
0 → 8
```

にできる。

通常の `new WordCollection()` は従来どおり通常Listで開始し、Lexer以外のcapacity policyは変更していない。

## Lazy Capacity-8のAllocation結果

Capacity最適化を含まない正式baselineと、追加instance fieldを持たない最終版を比較した。

大量口上構成では、

### 4→8 resize

```text
1,241,760
→
0
```

### Copied Word References

```text
13,032,984
→
8,065,944

-4,967,040
-38.11%
```

### sampled Word[]

```text
372,927,760
→
347,217,664

-25,710,096 bytes
-6.90%
```

### sampled List<Word>

```text
75,411,696
→
68,799,144

-6,612,552 bytes
-8.77%
```

### Process Total Allocation

```text
6,529,862,744
→
6,482,760,816

-47,101,928 bytes
-0.72%
```

`WordCollection` 本体の平均object sizeは、

```text
48 bytes
→
48 bytes
```

で増加していない。

初期prototypeではcapacity hint用のinstance field追加により48→56 bytesへ大型化したため採用を見送った。最終版では既存referenceのnull状態をLexerPendingとして利用し、追加instance fieldを持たない構造へ変更している。

この比較値もLazy Capacity-8単独の正式Before / After結果であり、WordCollection Compact化の -4.63% と単純合算しない。

速度効果は **UNPROVEN** とする。

## 並列ERB解析まわり

ERB解析はPrimaryParseや残り関数のScriptParseで並列処理を行う。

今回の性能改善では並列Parserの実行方式を大きく変更せず、既存の安全対策を維持した。

共有error flagの更新など、並列実行中に単純代入で競合し得る箇所についてはthread-safeな更新を使用する。

また、WordCollection Compact化によってMacroやParserのnode操作が必要になった場合も、`EnsureLinked()` で従来のLinkedList semanticsへ戻すため、並列解析のために構文解釈そのものを変更していない。

## 汎用ERB Profiling

性能調査で再利用できる汎用Profilerを残した。

`PERFORMANCE_METRICS` が有効な計測ビルドでは、`--ErbStartupProfile` / `--ErbStartupProfileMode` を利用して、ERB起動処理の詳細情報を取得できる。

主な計測対象は以下。

- ERB stage timing
- PrimaryParse
- ScriptParse
- file単位の読み込み・解析情報
- function単位の解析時間
- `SetArgumentToCalls`
- `ForceSetArgumentCalls`
- FunctionCode別件数
- Label数
- InitiallyParsed
- Remaining
- ParallelRemaining

特定Phaseだけで使用した以下のmeasurementは、役目を終えた時点で削除した。

- Strict Decimal eligibility
- WordCollection mutation / promotion
- List growth / resize / capacity
- Parallel warning diagnostics

通常ReleaseにはPhase固有measurementを残さない。

## 速度評価について

今回の一連の変更ではallocation削減は明確に確認できた。

一方、起動時間については実行順・CPU状態・OS側の影響と思われる揺れが大きく、短いrunでは改善して見えても、交互実行を増やすと同じ結果が再現しないケースがあった。

そのため、

```text
Allocation:
CONFIRMED

Compatibility:
PASS

Speed:
UNPROVEN
```

としている。

今回の変更を「起動時間が○%高速化した」とは扱わない。

## 互換性確認

開発・検証過程では、Normal構成と大量口上構成を用いて以下を確認した。

- Release Build: PASS
- `PERFORMANCE_METRICS` Build: PASS
- Normal Startup: PASS
- Kojo Startup: PASS
- Parser exceptionなし
- 対象検証でLv2 warningなし
- Pointer / Collection / EnsureLinked: PASS
- Macro実fixture: PASS
- self-add旧挙動: PASS
- Compact Add / Clone / Insert / Remove: PASS
- Lexer 0 / 1 / 4 / 5 / 8 / 9 token等の境界: PASS

大量runを行った過去のMacro warning調査とは目的が異なり、今回のallocation改善について速度改善率を確定するための大量benchmarkは行っていない。

## Macroハッシュ衝突修正との関係

2026-08-13に修正したMacroハッシュ衝突問題は、今回の性能改善とは別の修正である。

Macro辞書については最新main側の、

```text
d3c1d2e
Macroハッシュ衝突による低頻度ERB誤解析を修正
```

を正とする。

今回の性能改善移植では `IdentifierDictionary.cs` を上書きせず、最新mainの文字列key版Macro辞書を維持する。

詳細は、

```text
#今回の修正説明_2026-08-13.md
```

を参照。

## 残存Issue / 今後の候補

今回変更していない主な候補として、fresh profile上では以下が残っている。

- `InstructionLine`
- `CharStream`
- `System.String`
- `Char[]`
- `IdentifierWord`
- `AExpression[]`

これらは今回の変更に含めず、別の性能調査対象とする。

Parallel Warning調査で見つかった `VariableLocal.localVarTokens` のownership候補も別Issueであり、今回変更していない。

## 関連ソース

- `Runtime/Script/Data/DefineMacro.cs`
- `Runtime/Script/Loader/ErbLoader.cs`
- `Runtime/Script/Parser/LexicalAnalyzer.cs`
- `Runtime/Script/Parser/WordCollection.cs`
- `Runtime/Script/Statements/Expression/ExpressionParser.cs`
- `Runtime/Utils/ErbStartupProfiler.cs`
- `Runtime/Utils/EraStreamReader.cs`
- `Program.cs`

## 関連資料

- `プロジェクト資料/07_ERB起動詳細Profiling.md`
- `プロジェクト資料/08_ArgumentParser性能設計.md`
- `#今回の修正説明_2026-08-13.md`

