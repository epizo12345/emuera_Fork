# 今回の修正説明（2026-07-31）

このファイルは、取り込まれた.NET 10対応版を調査したあと、追加で直した問題だけを説明するものです。
行番号は2026-07-31時点のおおよその位置です。今後コードを追加・削除すると少しずれることがあります。

## 最初に結論

取り込まれた更新はおおむね動作していましたが、次の5点が残っていました。

1. SQLiteの既知の脆弱性を含む版が、間接的に選ばれる可能性があった。
2. .NET 10版SkiaSharpを使うためのWindows対象指定と、垂直同期の書き方が合っていなかった。
3. コマンドライン解析に、開発途中のプレビュー版パッケージを使っていた。
4. ソースは.NET 10版なのに、配布フォルダには古い.NET 9版EXEと説明が残っていた。
5. Windowsの対象版を明示したことで、Emueraが直接使わない約24 MiBのWindows SDK補助DLLまで単一EXEへ入っていた。

ゲームのERB、セーブデータ、ゲーム内処理を変更した修正ではありません。

## 1. SQLiteの既知の脆弱性

### 修正前の問題点

`Microsoft.Data.Sqlite`だけを指定すると、内部で使われる`SQLitePCLRaw`の2.1.11が間接的に選ばれることがありました。
この版はNuGetの検査で重大な既知の脆弱性として検出されます。

これは「ゲームを起動すると必ずエラーになる」という種類の問題ではありません。
普段は何も起きていないように見えますが、問題のあるSQLite処理を利用する状況が重なった場合に安全性上の危険が残るため、配布前に直す必要がありました。

### 修正意図

既知の脆弱性が報告されたSQLiteネイティブライブラリを、配布版へ含めないようにする。

### 修正内容

安全な`SQLitePCLRaw.bundle_e_sqlite3` 2.1.12を直接指定しました。
これにより、NuGetが古い2.1.11を間接依存として選ぶことを防ぎます。

### 修正箇所と大まかな行

- `Emuera.csproj` 84～87行付近
  - 修正理由のコメントを追加
  - `SQLitePCLRaw.bundle_e_sqlite3` 2.1.12の参照を追加

## 2. .NET 10版SkiaSharpと垂直同期の不一致

### 修正前の問題点

対象フレームワークが`net10.0-windows`だけになっており、対応するWindowsの最低バージョンが書かれていませんでした。
この状態では、SkiaSharp 4.150.1が用意している現在のWindows向け部品ではなく、古い.NET Framework向け互換部品が選ばれ、ビルド時に互換性警告が発生していました。

Windowsの最低バージョンを正しく指定すると現在のOpenTKが選ばれますが、今度は以前使っていた`mainPicBox.VSync`が存在しないため、そのままではビルドできません。
反対に、`VSync`の行を単純に消すだけでは垂直同期も失われ、画面のちらつきや無駄な描画が増える可能性があります。

### 修正意図

.NET 10用のSkiaSharp/OpenTKを正しく選びながら、従来の垂直同期も維持する。

### 修正内容

対象フレームワークを`net10.0-windows10.0.19041.0`に変更しました。
これはWindows 10 Version 2004（build 19041）以降を対象にする、という意味です。

古い`VSync`プロパティの代わりに、OpenGLの準備完了後に`Context.SwapInterval = 1`を設定する方法へ移しました。

### 修正箇所と大まかな行

- `Emuera.csproj` 3～7行付近
  - Windowsの最低バージョンを含む対象フレームワークへ変更
- `UI/Framework/Forms/EraPictureBox.cs` 13～20行付近
  - OpenGL準備後の垂直同期処理を追加
- `UI/Framework/Forms/MainWindow.Designer.cs` 613～614行付近
  - 存在しなくなった`mainPicBox.VSync = true`を削除
  - 垂直同期の移動先を示すコメントへ変更

## 3. System.CommandLineのプレビュー版依存

### 修正前の問題点

起動オプションを解析する`System.CommandLine`に、`3.0.0-preview.6`という開発途中の版を使用していました。
今回、このパッケージ自体に既知の脆弱性が見つかったわけではありません。

ただし、プレビュー版は正式な仕様が確定しておらず、将来の更新でAPIが変わったり、サポートされなくなったりする可能性があります。
個人配布する通常版でプレビュー版を使い続ける理由がないため、安定版へ戻しました。

### 修正意図

起動オプションの機能を変えずに、開発途中のパッケージへの依存をなくす。

### 修正内容

`System.CommandLine`を安定版2.0.10へ変更しました。
安定版ではオプションと通常引数の登録方法が異なるため、`rootCommand.Add(...)`を次のように置き換えました。

- オプション: `rootCommand.Options.Add(...)`
- 通常引数: `rootCommand.Arguments.Add(...)`

受け取る起動オプション、その値、ゲームの動作は変更していません。

### 修正箇所と大まかな行

- `Emuera.csproj` 94～96行付近
  - プレビュー版3.0.0から安定版2.0.10へ変更
- `Program.cs` 61～89行付近
  - 安定版APIに合わせてオプションと引数の登録方法を変更
  - 実際の登録変更は64、67、76、83、89行付近

## 4. ソースと配布フォルダの食い違い

### 修正前の問題点

ソースコードとテスト用ビルドは.NET 10へ更新されていましたが、`配布/Emuera.NET_最終通常版`には以前の.NET 9版EXEが残っていました。
ルートREADMEにも「配布フォルダは旧.NET 9版」と書かれていました。

このまま配布すると、ソースに入っている脆弱性対応、.NET 10対応、ダークモードなどが配布EXEには含まれず、説明と実物も一致しません。

### 修正意図

手動確認したEXE、ソース、必要ランタイム、説明文、改ざん確認用ハッシュをすべて一致させる。

### 修正内容

利用者がeramegatenPで手動確認した.NET 10版EXEを、そのまま配布フォルダへコピーしました。
配布READMEを.NET 10向けに変更し、採用済み修正ZIPとSHA-256一覧も現在の内容へ更新しました。

### 修正箇所と大まかな行

- `readme.md` 39～50行付近
  - 配布フォルダの説明を旧.NET 9版から現在の.NET 10版へ変更
- `配布/Emuera.NET_最終通常版/README.md` 1～56行付近
  - 対象OS、必要ランタイム、採用内容、導入方法を更新
- `配布/Emuera.NET_最終通常版/SHA256SUMS.txt` 1～2行
  - 配布EXEと採用済み修正ZIPのSHA-256を更新
- `配布/Emuera.NET_最終通常版/Emuera.exe`
  - 手動確認済み.NET 10版へ差し替え（バイナリファイルなので行番号なし）
- `配布/Emuera.NET_最終通常版_採用済み修正.zip`
  - 今回の修正を含む31ファイルで再作成（ZIPなので行番号なし）
- `更新履歴.md`
  - 新規作成のためファイル全体
- `今回の修正説明_2026-07-31.md`
  - 新規作成のためファイル全体

## 5. 単一EXEへ不要なWindows SDK補助DLLが入る

### 修正前の問題点

Windows 10 Version 2004以降を対象として正しく指定すると、Windows APIをC#から広く使うための
`Microsoft.Windows.SDK.NET.dll`も自動的に参照されます。

このDLLは約24 MiBあり、単一EXEへそのまま格納されていました。
そのため配布EXEは49,518,008バイト（約47.2 MiB）になっていましたが、Emuera自身はこの大きなDLLを直接使っていませんでした。

一方、SkiaSharp 4.150.1のWindows向け部品は、小さい`WinRT.Runtime.dll`を実行時に参照します。
大きなDLLと一緒に`WinRT.Runtime.dll`まで外すと、ビルドが成功しても描画初期化時にエラーになる危険がありました。

### 修正意図

Windows 10 Version 2004以降という対象OSとSkiaSharpの動作を維持したまま、
使っていない大きなWindows SDK補助DLLだけを配布EXEから外す。

### 修正内容

`IncludeWindowsSDKRefFrameworkReferences`を`false`にして、Windows SDKの.NET投影DLLを自動追加しないようにしました。

代わりに`Microsoft.Windows.CsWinRT` 2.2.0を直接参照しました。
ここから得られる`WinRT.Runtime.dll`は、修正前にWindows SDK経由で入っていたものと同一内容です。

さらに、このパッケージの`WinRT.Runtime.dll`には通常「発行先へコピーしない」という印が付くため、
パッケージ名とDLL名の両方が一致する1ファイルだけ、その印を発行時に上書きしています。
これにより、別DLLを誤って追加せず、必要なWinRT部品を単一EXEへ確実に格納します。

修正後のEXEは24,639,210バイト（約23.5 MiB）です。
利用者によるeramegatenPの起動・操作確認でも問題はありませんでした。

### 修正箇所と大まかな行

- `Emuera.csproj` 8～12行付近
  - Windows SDKの.NET投影DLLを自動追加しない設定
- `Emuera.csproj` 94～104行付近
  - `Microsoft.Windows.CsWinRT` 2.2.0の明示参照
- `Emuera.csproj` 129～145行付近
  - `WinRT.Runtime.dll`だけを単一EXEの発行対象へ戻す処理
- `配布/Emuera.NET_最終通常版/Emuera.exe`
  - 利用者確認済みの小型版へ差し替え
- `配布/Emuera.NET_最終通常版/README.md`
  - 新しいEXEサイズ、ハッシュ、同梱方針を追記
- `配布/Emuera.NET_最終通常版/SHA256SUMS.txt`
  - 配布EXEと採用済み修正ZIPのSHA-256を更新

### 参照

- [.NETの単一ファイル配置](https://learn.microsoft.com/dotnet/core/deploying/single-file/overview)
- [Microsoft.Windows.CsWinRT 2.2.0](https://www.nuget.org/packages/Microsoft.Windows.CsWinRT/2.2.0)
