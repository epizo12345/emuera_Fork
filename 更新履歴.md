# 更新履歴

このファイルは、配布版へ採用した主な変更を新しい順に記録するためのものです。
個々のコード変更の詳しい説明は、同じフォルダにある日付付きの「今回の修正説明」を参照してください。

## 2026-08-11 - ゲームパッド対応 v1（ブランチ候補）

### 入力デバイス

- XInputの0～3番を検索し、接続中のXbox系コントローラを自動選択するようにしました。
- XInputがない場合はWinMM joystick APIを使用し、`joyGetPosEx`が成功した実デバイスを採用します。
- WinMMで取得できないHIDゲームパッド向けにRaw Inputフォールバックを追加しました。
- `Microsoft PC ジョイスティック ドライバー`というデバイス名だけでは除外せず、軸・ボタンと実入力の成功を優先します。
- 起動後の接続、切断、再接続とAlt+Tab復帰を考慮し、入力状態を安全に再取得します。

### ゲームパッドUI

- 通常選択肢、HTML UI、HTML Island、INPUTMOUSEKEYを共通のNavigation Graphへ変換し、十字キー・左スティックで移動できるようにしました。
- ×は現在フォーカス中のボタンを通常決定と同じ経路で実行します。
- ○は現在のInputRequest内から意味上のCANCEL/BACKを検索し、×決定と同じ経路で実行します。候補がない場合だけEsc・右クリック相当へ戻します。
- 全角の`ＣＡＮＣＥＬ`、`ＢＡＣＫ`、`ＲＥＴＵＲＮ`はNFKC（FormKC）正規化により半角版と同じ意味で認識します。
- LB/RBログ操作、入力リピート、選択ハイライト、ダンジョン向け左スティック直接入力を追加しました。

### 起動・診断

- ゲームパッド初期化はEmuera本来の初回表示完了後へ分離し、コンストラクタ中のポーリングや初期表示前のRaw Input登録を廃止しました。
- 起動表示を隠すOpacity、Overlay、固定待機は使用していません。
- `--GamepadDebug`でデバイス列挙、使用API、軸・ボタン状態、Navigation Graph、決定・キャンセル経路を`gamepad-debug.log`へ記録できます。
- `--GamepadDirectInput`で直接入力プロファイルを選択できます。

### 文書と検証

- v1仕様は`プロジェクト資料/07_ゲームパッド対応_v1_仕様書.md`へ記録しました。
- 詳しい実装説明は`今回の修正説明_2026-08-11_ゲームパッド対応_v1.md`へ記録しました。
- Gitで追跡する配布用単一EXE、README、SHA-256一覧を`配布/Emuera.NET_ゲームパッド対応_v1/`へ追加しました。
- Releaseビルドは成功（エラー0件）。隔離データによるStartupTestModeは2回とも成功し、警告Lv2は0件でした。
- PS4互換ワイヤレスコントローラについて、WinMM認識、通常操作、全角CANCELの○操作は利用者の実機ログ・実機操作で確認済みです。最新の起動表示修正は実機での再確認待ちです。

## 2026-07-31 - .NET 10対応版の確認と配布更新

### 基盤・表示

- 対象フレームワークを.NET 10へ更新しました。
- Windows 11のダークモード対応を取り込みました。
- SkiaSharp 4.150.1を使用する構成に更新しました。
- OpenGLの垂直同期設定を、現在のOpenTKに合う方法へ移しました。
- Windows API全体の.NET投影DLLは同梱せず、SkiaSharpが必要とする`WinRT.Runtime.dll`だけを単一EXEへ含める構成にしました。
- 配布EXEを49,518,008バイト（約47.2 MiB）から24,639,210バイト（約23.5 MiB）へ削減しました。

### ログ表示

- CRER氏による`ClipBoardDialog`の高速化を取り込みました。
- 大量ログの設定中だけ途中再描画を止め、ログ内容を省略せずに表示を高速化しました。
- ログ表示欄を大量文字列に向く`RichTextBox`へ変更しました。
- Ctrl+Cとメニューの両方で、閉じたログ画面と保持中のログを確実に破棄するようにしました。

### 安全性・依存パッケージ

- SQLiteのネイティブライブラリを含むパッケージを2.1.12へ固定し、既知の脆弱性が報告された古い版を使わないようにしました。
- System.CommandLineをプレビュー版から安定版2.0.10へ更新し、呼び出しコードも安定版APIに合わせました。
- AngleSharp 1.6.0を使用する更新を取り込みました。
- `Microsoft.Windows.CsWinRT` 2.2.0を明示参照し、従来の大きなWindows SDK部品から使われていたものと同じ`WinRT.Runtime.dll`を保持しました。

### 説明・配布物

- 動作条件と現在の機能に合わせて、ルートのREADMEを更新しました。
- `配布/Emuera.NET_最終通常版/Emuera.exe`を、必要なWinRT部品だけを含む.NET 10対応版へ更新しました。
- 採用済みソースだけを収めるZIP、配布README、SHA-256一覧を現在の内容に合わせて更新しました。

### 確認結果

- Releaseビルド: 成功（エラー0件）
- NuGet既知脆弱性検査: 検出0件
- NuGet非推奨パッケージ検査: 検出0件
- 単一EXE内部: `WinRT.Runtime.dll`あり、`Microsoft.Windows.SDK.NET.dll`なし
- eramegatenP: 利用者による手動起動・操作確認で問題なし
- ログ表示: 利用者がCtrl+C・メニュー経路を手動確認し、問題なし

## 2026-07-15 - 最終通常版として採用した既存改修

- 起動時にまれに発生していた、ERBのラベルや関数を未定義と誤判定する警告を抑止しました。
- ERBの解析処理を中心に起動時間を短縮しました。
- マクロ中のゲーム処理と出力内容を維持しつつ、画面更新をまとめてマクロ実行を高速化しました。
- マウスの「戻る」「進む」サイドボタンから、画面内の前ページ・次ページ操作を行えるようにしました。
- 既存のセーブデータ、ERB、ERH、CSV、設定ファイルを変更せずに利用できる方針を維持しました。
