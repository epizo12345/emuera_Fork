# 更新履歴

このファイルは、配布版へ採用した主な変更を新しい順に記録するためのものです。
個々のコード変更の詳しい説明は、同じフォルダにある日付付きの「今回の修正説明」を参照してください。

## 2026-08-11 - ゲームパッドNavigation追加修正（Return Focus History）

- ×で別画面へ進んだ後、○のsemantic Backで戻ると遷移前にFocusしていたTargetを復元する、汎用のReturn Focus StackとLast Focus Cacheを追加しました。ゲーム側ERB/CSV、Input番号、画面固有の条件分岐には依存しません。
- 画面はmodal scope、Input、正規化ラベル、Source/Layout/Base group、NavigationGroup、Back属性、Boundsを含むFingerprintで照合します。同じ`input=0`だけが偶然一致する別画面や、modalの前面・背景を誤復元しません。
- 復元順はmodal前面scope、Confirm直後のPostConfirm Anchor、Return Stack、Last Focus Cache、通常初期Focusです。A→B→C→B→Aの入れ子、Target消失時の同一縦レーン・近傍への安全なフォールバック、少数の動的項目増減を扱います。
- Return Stackは最大24件、Cacheは最大48画面に制限し、LB/RBのPage Navigationは履歴へ記録しません。`--GamepadDebug`にStack push/pop、Fingerprint一致・拒否、Cache/代替Targetの選択を記録し、自己テストA～Iを追加しました。
- Release単一EXEを24,753,898 bytes（SHA-256: `7DCFA38C8C6ADABA31D26A7C1AE05488C9427286F3ECC1C05B7B2644AB3CABAC`）へ更新しました。StartupTestModeは2/2成功、警告Lv2 0件でした。
- Chatモード引き継ぎ用に`GamepadHandoff_20260811_ShoulderPageNavigation.zip`を更新しました。

## 2026-08-11 - ゲームパッドNavigation追加修正（LB/RB Indexed / Directional Page Navigation）

- ShinEraTenseiPの実ページUIを確認し、`PRINTBUTTON @"[{LCOUNT+100}] PAGE.{LCOUNT}"`で生成されるページTargetを、入力値100/101に依存せず表示ラベルから検出するようにしました。`[200] ヒント`などPAGE形式でないボタンは含めません。
- FormKC正規化後の`PAGE.0` / `PAGE 0`形式、同一Source/Base group/NavigationGroup、2個以上かつ一意な番号、現在ページだけの非デフォルト表示スタイルをすべて満たす時だけ、LB/RBを前/次ページに割り当てます。現在ページが一意でない場合は推測せずログスクロールを維持します。
- PAGE Targetの実行は既存のConsoleButton confirm path / virtual-left-clickへ渡します。先頭のLB・末尾のRBは何もしません。PostConfirm Anchorへ旧ページを残さず、PAGE ButtonへFocus履歴が吸われないようにしました。
- 「前のページ」「次のページ」「後ろのページ」等の明示的なDirectional TargetもFormKC正規化後の表示ラベルから検出します。「戻る」「BACK」「RETURN」「CANCEL」「次へ」はページ移動とみなしません。片方向Targetが1個だけのページ端でも有効にします。
- LB/RBの優先順位を`Indexed → Directional → Log Scroll`とし、Directional型で該当方向のTargetがない場合はconsume-no-opにしてログスクロールへ流さないようにしました。Targetの実行はIndexed型と同じ既存共通経路を使い、Input番号は疑似送信しません。
- HTML modalが前面にある場合はforegroundだけを検索し、背後PAGEを操作しません。`--GamepadDebug`へIndexed/Directionalの検出、切替先、端、consume-no-op、フォールバック理由を記録し、自己テストCase A～FとDirectional A/B/C/D/E/Hを追加しました。
- Release単一EXEを24,741,610 bytes（SHA-256: `65C0AD524F627655CE8E3CFDAE99BE6AEA2EC3067916890F525E72B42F13970C`）へ更新しました。StartupTestModeは2/2成功、警告Lv2 0件でした。
- Chatモード引き継ぎ用に`GamepadHandoff_20260811_ShoulderPageNavigation.zip`を作成しました。

## 2026-08-11 - ゲームパッドNavigation保守修正（HTML modal popupのFocus scope）

- HTML Islandの`PrintHTMLIsland(depth)`による描画深度を使い、前面のmodal選択肢と背面のCancel backdropを相対的に識別するようにしました。深度番号の固定判定はしていません。
- viewportの大部分を覆うHTML IslandのBack/Cancel Targetが、より前面の小さな非Back選択肢群と同時に存在する場合、backdropをDirectional Navigation Graphから除外し、foreground modal groupだけを通常Focus scopeにします。Mouse Hit Testと○Cancel候補には残します。
- popup表示直後はforegroundの最初のenabled non-Back TargetへFocusし、[0]はい→[1]いいえの上下移動と×決定を可能にしました。日本語`input=キャンセル`もsemantic Backとして扱います。
- `--GamepadDebug`へmodal scope、backdrop除外、初期modal Focus、Modal Cancelを記録し、Case A～Dの自己テストを追加しました。LB/RB PAGE切替や入力取得層は変更していません。
- Release単一EXEを24,729,322 bytes（SHA-256: `EB4F8E7AE96C8D517531AA1C1F8E6C43FD8CBC3470A0407DDF9DAA03C3D22E33`）へ更新しました。
- Chatモード引き継ぎ用に`GamepadHandoff_20260811_HtmlModalPopup.zip`を作成しました。

## 2026-08-11 - ゲームパッドNavigation保守修正（論理ボタン断片の統合）

- 実機ログで確認された、同じ入力番号を持つ`IsButton=true`の複数行断片を、説明文として除外するのではなく1つの論理ボタンへ畳み込むようにしました。前回の除外修正だけでは、これらが実際にクリック可能な断片だったため対象に残っていました。
- 同一`ConsoleButtonString`は従来どおり同一オブジェクトとして統合し、別インスタンスへ分割された断片は、同じInput、Generation、Source/Layout/Base group、連続行、近接したX/Y/幅を満たす場合だけ統合します。同じInputの遠い別ボタンは統合しません。
- 論理ボタンの代表は最上段・最初の断片とし、矩形だけをUnionします。これによりFocusのハイライト範囲を保ち、×決定・○のsemantic Backとも元の`ConsoleButtonString`実行経路を維持します。LB/RB、入力取得層、Navigation方式は変更していません。
- `--GamepadDebug`へ`Logical button group`ログを追加し、Input、断片数、代表行、構成行を記録します。自己テストで0/1/2の各4断片、0→1→2リンク、代表保持、遠い同一Inputの非統合を確認します。
- Release単一EXEを24,721,130 bytes（SHA-256: `AD88C344970D4EAD5114854309E4890742A4BE17BD948ED67BC50F59FBB58543`）へ更新し、自己テストPASSを確認しました。
- Chatモード引き継ぎ用に`GamepadHandoff_20260811_LogicalButtonFragments.zip`を作成しました。

## 2026-08-11 - ゲームパッドNavigation保守修正（非インタラクティブ説明文の除外）

- 通常ConsoleのFocus Targetを、表示ツリーの子テキスト単位ではなく、Emueraが実際にクリック・決定可能として生成した`ConsoleButtonString.IsButton`単位で収集するようにしました。
- `input=0`は除外条件に使わず、`IsButton=true`の正常な0番選択肢は保持します。`IsButton=false`の説明文・装飾文・空行はTargetにしません。
- HTML_PRINT、HTML Island、INPUTMOUSEKEYの既存クリック領域、ON/OFFのPostConfirm Focus Persistence、LB/RBログ処理、入力デバイス層は変更していません。
- 自己テストへ、A: 説明文を挟む0→1、B: Input=0実ボタン、C: 0相当の通常テキスト、D: HTMLボタン間の通常テキストを追加しました。
- Release単一EXEを24,717,034 bytes（SHA-256: `126570B922829FCDAABC42784E849CF2B9C317D4F9CBD7E7B16075EAC95D3197`）へ更新し、StartupTestMode 2/2成功・警告Lv2 0件を確認しました。
- Chatモード引き継ぎ用に`GamepadHandoff_20260811_NonInteractiveFocusTargets.zip`を作成しました。

## 2026-08-11 - ゲームパッドUI保守修正（ON/OFF設定画面のフォーカス維持）

- ON/OFF切替後にERBが同じ設定画面を再描画して新しいInputRequestを作っても、決定直前の対象を一時的なPostConfirm Focus Anchorとして記録し、同一画面と判定できる場合だけ同じ項目へフォーカスを復元するようにしました。
- 同一画面判定は、入力値だけでなくSource/Base group、表示Bounds、Back属性、対象数と一致率を組み合わせます。別画面で同じ入力番号が再利用されても、入力番号だけでは復元しません。
- 対象が切替後に一時的に消えた場合は同じ縦レーンの下・上・近傍を順にフォールバックし、画面構造が変わった場合は通常の初期フォーカスへ戻します。
- Anchorは×決定の直後だけ作成し、○キャンセルの既存経路やゲームパッド入力取得層は変更していません。`--GamepadDebug`へAnchor、次のRequest、同一画面比較、復元・拒否理由を記録します。
- 自己テストへ、A: ON/OFF切替、B: 連続切替、C: 別画面拒否、D: 同一入力番号の別画面拒否を追加しました。
- Release単一EXEを24,717,034 bytes（SHA-256: `22599F2C4D4E1CCA592E92ED272E44599C15CD551A5581E9AD0E58C2D1174699`）へ更新し、StartupTestMode 2/2成功・警告Lv2 0件を確認しました。
- Chatモード引き継ぎ用に`GamepadHandoff_20260811_ONOFFFocusPersistence.zip`を作成しました。

## 2026-08-11 - ゲームパッド対応 v1 保守修正（レビュー反映）

- Raw InputのHat/POV（十字キー）とX/Y軸（左スティック）を別の方向ソースとして保持し、左スティックでも既存のダンジョン直接入力へ到達できるようにしました。
- Navigation Graphの診断を、正しい同一レーンのRow skipを警告しない検証へ修正しました。逆方向リンクは引き続き警告します。
- Raw InputでPS4系が見つかったというPC全体の状態を、別のWinMMデバイスのボタン配列へ流用しないようにしました。WinMM自身の識別情報または一意な対応付けで確認できる場合だけPS4配列を選びます。
- `--GamepadLayout Auto|Xbox|PlayStationWinMM`（または`EMUERA_GAMEPAD_LAYOUT`）を追加し、APIで安全に識別できない特殊なパッドには明示指定を用意しました。
- `--GamepadDebug`へ接続時のFaceButtonLayout、選択理由、Raw InputのDPad/LeftStickを記録するようにしました。診断時の自己テストで上記3件の回帰ケースを確認します。

## 2026-08-11 - ゲームパッド対応 v1（ブランチ候補）

### 入力デバイス

- XInputの0～3番を検索し、接続中のXbox系コントローラを自動選択するようにしました。
- XInputがない場合はWinMM joystick APIを使用し、`joyGetPosEx`が成功した実デバイスを採用します。
- WinMMで取得できないHIDゲームパッド向けにRaw Inputフォールバックを追加しました。
- `Microsoft PC ジョイスティック ドライバー`というデバイス名だけでは除外せず、軸・ボタンと実入力の成功を優先します。
- 起動後の接続、切断、再接続とAlt+Tab復帰を考慮し、入力状態を安全に再取得します。

### ゲームパッドUI

- 通常選択肢、HTML UI、HTML Island、INPUTMOUSEKEYを共通のNavigation Graphへ変換し、十字キー・左スティックで移動できるようにしました。
- ×は現在フォーカス中のボタンを通常決定と同じ経路で実行します。
- ○は現在のInputRequest内から意味上のCANCEL/BACKを検索し、×決定と同じ経路で実行します。候補がない場合だけEsc・右クリック相当へ戻します。
- 全角の`ＣＡＮＣＥＬ`、`ＢＡＣＫ`、`ＲＥＴＵＲＮ`はNFKC（FormKC）正規化により半角版と同じ意味で認識します。
- LB/RBログ操作、入力リピート、選択ハイライト、ダンジョン向け左スティック直接入力を追加しました。

### 起動・診断

- ゲームパッド初期化はEmuera本来の初回表示完了後へ分離し、コンストラクタ中のポーリングや初期表示前のRaw Input登録を廃止しました。
- 起動表示を隠すOpacity、Overlay、固定待機は使用していません。
- `--GamepadDebug`でデバイス列挙、使用API、軸・ボタン状態、Navigation Graph、決定・キャンセル経路を`gamepad-debug.log`へ記録できます。
- `--GamepadDirectInput`で直接入力プロファイルを選択できます。

### 文書と検証

- v1仕様は`プロジェクト資料/07_ゲームパッド対応_v1_仕様書.md`へ記録しました。
- 詳しい実装説明は`今回の修正説明_2026-08-11_ゲームパッド対応_v1.md`へ記録しました。
- Gitで追跡する配布用単一EXE、README、SHA-256一覧を`配布/Emuera.NET_ゲームパッド対応_v1/`へ追加しました。
- Releaseビルドは成功（エラー0件）。隔離データによるStartupTestModeは2回とも成功し、警告Lv2は0件でした。
- PS4互換ワイヤレスコントローラについて、WinMM認識、通常操作、全角CANCELの○操作は利用者の実機ログ・実機操作で確認済みです。最新の起動表示修正は実機での再確認待ちです。

## 2026-07-31 - .NET 10対応版の確認と配布更新

### 基盤・表示

- 対象フレームワークを.NET 10へ更新しました。
- Windows 11のダークモード対応を取り込みました。
- SkiaSharp 4.150.1を使用する構成に更新しました。
- OpenGLの垂直同期設定を、現在のOpenTKに合う方法へ移しました。
- Windows API全体の.NET投影DLLは同梱せず、SkiaSharpが必要とする`WinRT.Runtime.dll`だけを単一EXEへ含める構成にしました。
- 配布EXEを49,518,008バイト（約47.2 MiB）から24,639,210バイト（約23.5 MiB）へ削減しました。

### ログ表示

- CRER氏による`ClipBoardDialog`の高速化を取り込みました。
- 大量ログの設定中だけ途中再描画を止め、ログ内容を省略せずに表示を高速化しました。
- ログ表示欄を大量文字列に向く`RichTextBox`へ変更しました。
- Ctrl+Cとメニューの両方で、閉じたログ画面と保持中のログを確実に破棄するようにしました。

### 安全性・依存パッケージ

- SQLiteのネイティブライブラリを含むパッケージを2.1.12へ固定し、既知の脆弱性が報告された古い版を使わないようにしました。
- System.CommandLineをプレビュー版から安定版2.0.10へ更新し、呼び出しコードも安定版APIに合わせました。
- AngleSharp 1.6.0を使用する更新を取り込みました。
- `Microsoft.Windows.CsWinRT` 2.2.0を明示参照し、従来の大きなWindows SDK部品から使われていたものと同じ`WinRT.Runtime.dll`を保持しました。

### 説明・配布物

- 動作条件と現在の機能に合わせて、ルートのREADMEを更新しました。
- `配布/Emuera.NET_最終通常版/Emuera.exe`を、必要なWinRT部品だけを含む.NET 10対応版へ更新しました。
- 採用済みソースだけを収めるZIP、配布README、SHA-256一覧を現在の内容に合わせて更新しました。

### 確認結果

- Releaseビルド: 成功（エラー0件）
- NuGet既知脆弱性検査: 検出0件
- NuGet非推奨パッケージ検査: 検出0件
- 単一EXE内部: `WinRT.Runtime.dll`あり、`Microsoft.Windows.SDK.NET.dll`なし
- eramegatenP: 利用者による手動起動・操作確認で問題なし
- ログ表示: 利用者がCtrl+C・メニュー経路を手動確認し、問題なし

## 2026-07-15 - 最終通常版として採用した既存改修

- 起動時にまれに発生していた、ERBのラベルや関数を未定義と誤判定する警告を抑止しました。
- ERBの解析処理を中心に起動時間を短縮しました。
- マクロ中のゲーム処理と出力内容を維持しつつ、画面更新をまとめてマクロ実行を高速化しました。
- マウスの「戻る」「進む」サイドボタンから、画面内の前ページ・次ページ操作を行えるようにしました。
- 既存のセーブデータ、ERB、ERH、CSV、設定ファイルを変更せずに利用できる方針を維持しました。
