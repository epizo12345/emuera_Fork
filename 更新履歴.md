# 更新履歴

このファイルは、配布版へ採用した主な変更を新しい順に記録するためのものです。
個々のコード変更の詳しい説明は `プロジェクト資料/修正履歴/` にある日付付きの「今回の修正説明」を参照してください。

### 2026-08-25
- Controller/Gamepad対応版をR41I本体へ同期した正式版を更新しました。MaxLog到達後に方向入力が効かなくなる問題、Mapping下部の[0]/[1]/[9] navigation、DungeonのDirect Input後にFocus位置が失われる問題、Controller接続時のMacro大幅低速化を修正しました。WinMM/XInput使用中の不要なRaw Input payload解析を抑制し、Raw Input fallback、hotplug、Macro中の△/YによるEscape相当処理を維持しています。実機でhotplug、長時間Dungeon移動、Mapping、Macro中Escape、接続時Macro性能を確認済みです。既知事項として、Dungeon→戦闘→Dungeon復帰時にFocusが上側へ戻る場合がありますが、今回は修正対象外です。
- Phase 13R41Iを正式採用しました。Lazyから到達した未解析Eager関数のDeferred Eager処理、同一Lazy fileの初回ready判定、runtime parse後のwarning/error処理、#DIM/#DIMS transaction、Rename安全化、active Lazy/Deferred state時のRELOADERB全体再読み込み昇格、IgnoreUncalledFunction・未呼出警告・ReduceArgumentOnLoad・Debug・Analysis条件での安全側制限、ReduceArgumentOnLoad=ONCE更新判定を反映しました。correctness validation、save39 Request/Event/調教口上/save-load、save219/Repeat、settings matrix 9/9、ShinEraTenseiPのRELOADERB文字列0件を確認済みです。Repeat1000は約1.05%高速側、startup Wall差は約+0.36%で、性能はR41C比で実質維持です。正式EXEは24,659,690 bytes、ProductVersionは`0.2.6.0+888c57406e68f63274a4fa64e861923bc9ce7e69`、SHA-256は`F83D7EDBB3FEC91B4E92C8CFAB69A7149F92B5BE31E4CBE3DCB0DDA94EDE7EA0`です。

### 2026-08-23
- Phase 13R41Cとして、配布EXEのProductVersion suffixをEXE生成元source commitへ整合させました。未commit sourceをpublishしたため直前HEADが埋め込まれていた原因を確認し、clean source/tooling commitからpublishしてfull SHA検証を追加しました。ゲーム機能は変更していません。正式EXEは24,659,690 bytes、ProductVersionは`0.2.6.0+e8f836979288c303906ca78ba89738c0456d5e49`、SHA-256は`E94BAE6269BE6DC101FC5D7F4E5BE0719041769D1BD447C15EB7183EBE91FEC4`です。
- Phase 13R41B2として、`ERB/RPG/イベント`をLazy ERBの新しい既定対象へ追加しました。save39のイベント一覧表示後も596 files / 8,255,909 bytesが未接触で、Trial startup Working Set中央値は約69.2MB減少しました。既存の明示的なLazy対象は自動変更せず、イベント30「神野の娘」本編、save/load、save219実戦闘を実機確認しました。Repeat1000では安定した性能悪化を確認していません。正式EXEは24,659,690 bytes、SHA-256は`BA047D6D83A151839B107022BAD81B98DC5D894C8A35699FB1119BE0EC5A3A93`です。
- Phase 13R41B1として、`ERB/RPG/依頼`をLazy ERBの新しい既定対象へ追加しました。既存の明示的なLazy対象設定は変更せず、欠落・null・新規設定のdefaultだけを更新します。Request一覧、依頼5「教授の隠れ家」本編、初回hydration、save/load、save219実戦闘を実機確認し、Repeat1000では安定した性能悪化を確認しませんでした。正式EXEは24,659,690 bytes、SHA-256は`7CA2A3F6FCF5690F710EC47F6B07FB3C1EC08EF83A5F296DC1BD312F3E4BF499`です。
- Phase 13R40.3として、`Data\setting.json`から設定できる日本語名のLazy ERB設定を正式採用しました。旧`LazyErb`形式から自動移行し、Data基準の`ERB\`配下だけを対象に、不正パスやERB root指定を拒否します。Preload時の不要なbulk cacheを抑制し、従来互換性とreload safetyを維持しました。既存検証でManaged memory約1.311GB→約1.050GB、Working Set約2.224GB→約1.604GBを確認しています。正式EXEは24,659,690 bytes、SHA-256は`E781D64460F0C0D13AAA1140FD7E1E04522EAC7BF1753022622AA5C5F027F886`です。
- Phase 13R39として、`Data\ERB\口上\口上まとめ\`配下だけをLazy ERB Hydration化しました。起動時は関数stub／metadataだけを登録し、本文は初回実行直前にERBファイル単位でhydrateします。実測でKOJO startup Managedを`1,762,740,872`から`1,174,959,288 bytes`へ削減（-33.34%）し、通常ERB、AnalysisMode、DebugMode、save format、GC policyは変更していません。実機で大型口上、複数キャラ、調教反復、セーブ→再起動→ロードを確認しました。正式EXEは24,651,498 bytes、SHA-256は`0E135D2773776B2528BC3DEB25A6B1BAD1922BFB4DFB7780CB58F97F75BC2C15`です。
- Phase 13R39.1として、口上まとめ外のpartial/folder reloadでactive Lazy managerを置換せず、通常モードで口上まとめを含む実対象はfull reloadへ昇格するよう修正しました。DebugMode / AnalysisModeではLazy保護目的の昇格を行わず、従来のpartial/folder reloadを維持します。preprocessorはeager fallbackとし、`[[...]]` renameは維持しました。save219 + RepeatCount=100、Release 0 errors / 35 existing warnings / new warning 0を確認しました。正式EXEは24,651,498 bytesです。

### 2026-08-22
- Phase 13R38として、通常表示ログの`displayLineList`をring buffer化しました。`MaxLog=50000`到達後の先頭破棄をO(n)の`List.RemoveAt(0)`からO(1)へ変更し、論理index順、描画、バックログ、選択肢、save/ERB semanticsを維持しました。save219後RepeatCount=1000でdisplayLineCount=50000、killed=false、応答ありを確認しました。正式EXEは24,647,402 bytes、SHA-256は`EE4D905209B241709AC69375FDA4747F4A52917D2EC47808D3A986FD712E1855`です。
- Phase 13R37として、`LogicalLine`内部の`ScriptPosition`をfileId/lineNoへ圧縮し、同一ERBのFilename参照slotを全行から除去しました。外部位置情報、default/new/null、reload、raw source、save互換性を維持し、`InstructionLine` shallow sizeを80 bytesから72 bytesへ削減しました。正式EXEは24,643,306 bytes、SHA-256は`67AD3118932E09EDDABDDB48AE7E0FE0D6F59451FDB91C056A4393553FB6388A`です。
- Phase 13R36として、`InstructionLine`の`FunctionIdentifier`専用参照slotを削除し、`FunctionCode`とassignment `OperatorCode`を1つの`int`へpackしました。built-in lookup、SET、method-as-instruction identity、lazy parsing、save互換性を維持し、shallow sizeを88 bytesから80 bytesへ削減しました。InputReady heap censusでmanaged liveは`1,819,500,051`から`1,786,073,667 bytes`、`InstructionLine`は`343,417,008`から`312,197,280 bytes`へ変化しました。正式EXEは24,643,306 bytes、SHA-256は`20B14927B21AF4C38A5B99CB1CCBDC8C3F04208C74C13D3C0A16AB4D82F13701`です。
- Phase 13R35として、`InstructionLine`のerror messageをR34の`argumentStorage`へunionし、`InstructionLine` shallow sizeを96 bytesから88 bytesへ削減しました。R34→R35同一InputReady heap censusでmanaged liveは`1,851,447,905`から`1,818,291,905 bytes`へ、`InstructionLine`は`374,636,736`から`343,417,008 bytes`へ変化しました。正式EXEは24,643,306 bytes、SHA-256は`C4D73CD86784F1F60299703E8F57BD5BBF988440FAD9422183622BBC25EE8376`です。
- Phase 13R34として、lazy argument parsing前の`CharStream`保持をsource/offset snapshotへ変更し、reader単位の一時stream再利用を追加しました。InputReady heap censusで`CharStream`は`1,960,514 objects / 62,736,448 bytes`から`0 objects / 0 bytes`、managed liveは`1,911,258,161`から`1,848,434,565 bytes`へ変化しました。`InstructionLine` 96-byte layout、lazy parsing、save互換性は維持しています。正式EXEは24,643,306 bytes、SHA-256は`84A999C7A6336F46DB6655368A482347D7A78CEBBA349F125196EB8BA7969222`です。
- Phase 13R32として、`LogicalLine`の`isError`と`errMes`を`errMes` 1slotへ統合しました。正常行はnull、error行は空文字またはmessageで表現し、lazy parser・InvalidLine/InvalidLabelLine・CALL error伝播は維持しています。
  - R31→R32候補heap censusで`InstructionLine`は同数3,902,466 objects、shallow 104→96 bytes、`405,856,464`→`374,636,736` bytes（差分`-31,219,728 bytes`、約29.77 MiB）を実測しました。
  - managed live総量は測定状態がR31基準と揃わないためR32単独差分は未確定です。Release 0 errors / 35 existing warnings / new warning 0、save219ロード後RepeatCount=100 smokeを完了しました。
  - 正式single-file配布版を更新しました。配布EXEは24,643,306 bytes、SHA-256は`1B1D3A826590D7AF5D1E65658F7AA1911C15D2BB7003ED650DB99E98497B4C24`です。
- Phase 13R31として、`LogicalLine`内部の`ScriptPosition?`専用slotを非nullable`ScriptPosition`と`Filename == null` sentinelへ変更しました。公開`Position` nullable semantics、明示的`new ScriptPosition()`、label/error/reload経路、save互換性は維持しています。
  - 候補版heap censusで`InstructionLine`は同数3,902,466 objectsのまま、shallow 112→104 bytes、`437,076,192`→`405,856,464` bytes（差分`-31,219,728 bytes`、約29.77 MiB）を実測しました。
  - managed live総量は測定状態がR30基準と揃わないためR31単独差分として未確定です。Release 0 errors / 35 existing warnings / new warning 0、正式EXEのstartup・save219・RepeatCount=100 smokeを完了しました。
  - 正式single-file配布版を更新しました。配布EXEは24,643,306 bytes、SHA-256は`93F184794420DAD826AC7CECF58AE265D575973D42C4D930B3CE882BF0427950`です。
- Phase 13R30として、`InstructionLine`のSET左辺`assigndest`専用参照slotを削除し、解析前の左辺`WordCollection`を既存`auxiliaryData`へ一時保持するよう変更しました。
  - SETの左辺→右辺解析順、lazy argument parsing、IF / PRINTDATA / TRYCALLLIST / EndCatchのunion利用は維持しました。
  - retained censusで`InstructionLine`は`468,295,920` bytesから`437,076,192` bytesへ削減（同数3,902,466 objects、shallow 120→112 bytes）しました。
  - Release 0 errors / 35 existing warnings / new warning 0、InputReady・save219・RepeatCount=100 smokeを完了し、正式single-file配布版を更新しました。配布EXE SHA-256は`983713FB3FBB1E805291DAA11EFC9D17FE5F5600518803F6D7E3DD15407B6673`です。
- Phase 13R29として、parsed ASTのinteger literal経路で`SingleLongTerm.FromValue`を使い、R25の-1〜255 immutable cacheを再利用するよう変更しました。
  - `ExpressionParser.TermStack.Add(long)`だけを変更し、cache外は従来どおり新規instance、InstructionLine・評価順・save形式は維持しました。
  - managed live heapは`2,073,037,181` bytesから`2,009,815,855` bytesへ、`SingleLongTerm`は`83,040,896` bytesから`19,066,272` bytesへ減少しました（同一fixture条件のretained census）。
  - Release 0 errors / 35 existing warnings / new warning 0、factory境界値・InputReady・save219・RepeatCount=100 smokeを完了し、正式single-file配布版を更新しました。配布EXEは24,643,306 bytes、SHA-256は`948D71D0FCAE66B623A298A0EC924379A441F7230D6A6BFA294EFA24BD46B04F`です。
- Phase 13R28として、runtimeの`SingleStrTerm`は空文字列だけをimmutable singletonで共有するよう変更しました。
  - `VariableTerm`、`FunctionMethod`、UDF fallback、`StrFormTerm`などのruntime value-wrapperをfactoryへ移行しました。
  - `null`と非empty文字列は従来どおり個別instanceとし、任意文字列cache・interning・poolingは採用していません。
  - identity/mutability audit、focused確認、Release 0 errors / 35 existing warnings / new warning 0、InputReady・save219・RepeatCount=100 smokeを完了しました。
  - 正式single-file配布版を更新しました。配布EXEは24,643,306 bytes、SHA-256は`DBECAA7077624A3C0EB70527C5580F6DB8CBE53368432ABCE8F009EAAE5A8F6F`です。

### 2026-08-21
- Phase 13R27として、`UserDefinedFunctionArgument`の整数・文字列・REF transporterをdestination signatureに応じて必要時だけ確保するよう変更しました。
  - REF判定用`isRef`配列も削除し、CALL / REF semanticsを維持しました。
  - Release 0 errors / 35 existing warnings、InputReady・save219・RepeatCount=100 macro smokeを確認しました。
- Phase 13R26として、missing function / event lookupで対象が存在する場合だけ`CalledFunction`を生成するよう変更しました。
  - TRY系missing-targetの不要な一時allocationを削減し、成功時のCALL / event semanticsは維持しました。
  - Release build 0 errors / 35 existing warnings、InputReady・save219・RepeatCount=100 macro smokeを確認しました。
- Phase 13R25として、`SingleLongTerm`の-1〜255をimmutable固定cacheで共有する変更をProductionへ採用しました。
  - `VariableTerm.GetValue` / `FunctionMethod.GetReturnValue`の短命整数term allocationを削減し、cache外は従来どおり新規instanceを生成します。整数semanticsは変更していません。
  - cache境界・identity/mutability・Release build・口上有りfixtureのInputReady smokeを確認しました。
  - 正式single-file配布版を更新しました。配布EXEのhashはpublish後に記録します。
- Phase 13R22として、`VariableToken.CheckElement`の全要素check maskを共有するCandidate AをProductionへ採用しました。
  - 引数なし`CheckElement`経路で毎回生成していた3要素`bool[]`を静的共有配列へ置き換え、各overrideがmaskをread-only参照する既存契約を維持しました。
  - Release build 0 errors / 35 existing warnings。既存macro性能検証スクリプトでsave219ロード後のRepeatCount=100 smokeを完了しました。
  - 正式single-file配布版を更新しました。配布EXEは24,643,306 bytes、SHA-256は`C3DEEB31B42667D3353D09427FF07055564A5D7FA6B17EB85B2BA7FDE1F72A73`です。
- Phase 13R23として、`PrivateInt1DVariableToken`のtop-level private dynamic 1D整数配列をbounded reuseする変更をProductionへ採用しました。
  - 最大32,768要素まで再利用前にclearし、default値を復元します。nested / recursive呼出しは独立配列を使い、大きな配列は保持しません。
  - Release build 0 errors / 35 existing warnings、default/reuse/nested/32K focused確認、save219ロード後のRepeatCount=100 smokeを完了しました。
  - 正式single-file配布版を更新しました。配布EXEは24,643,306 bytes、SHA-256は`F7E145FB01E574A94C27221BE3B370EACFB3A130B7B60BD220A93B18BC034191`です。
- Phase 13R24として、`GetFixedVariableTerm`の短命runtime caller 10件を既存`RentFixedVariableTerm` lease/pool経路へ移行しました。
  - SWAPの2参照を含め、添字評価順を維持し、method scope内だけでleaseを使用します。early break / exceptionでもusingにより返却されます。
  - Release build 0 errors / 35 existing warnings、既存fixtureのsave219ロード後RepeatCount=100 smokeを完了しました。
  - 正式single-file配布版を更新しました。配布EXEは24,643,306 bytes、SHA-256は`001E0E4E6DB4C0D42EB56294194474A3BF017B38115EC4707FCA0A70CDB4B31D`です。
- Phase 13R14として、`ExpressionParser.TermStack`のinline-one-element storageをProductionへ採用しました。
  - 1要素だけを扱う場合は従来の`Stack<object>`と初期配列を必要になるまで生成せず、2要素目で従来同等のStackへpromotionします。
  - 大規模口上fixtureのScriptParse中の一時allocationを約251.4MB / 9.60%削減しました。常駐memoryや通常プレイplateauの削減量ではありません。
  - R13のServer/Workstation各10+10 run、Normal/Kojo、semantic validationを通過し、Serverの再現性ある起動退行は確認されませんでした。
  - Release 0 errors / 35 existing warnings / new warning 0。Workstation GCやProduction GC policyは変更していません。
  - 正式single-file配布版を更新しました。詳細は`プロジェクト資料/調査履歴/07_ERB起動詳細Profiling.md`を参照してください。

### 2026-08-19
- Phase 12B1.1 runtime allocation optimizationをProductionへ採用しました。
  - `PrivateInt2DVariableToken`のprivate dynamic 2D整数配列を、top-level通常呼出で最大1個再利用。
  - 再利用前に`Array.Clear`を行い、nested / recursive callは従来どおり独立した新規配列経路を維持。
  - 32,768要素以下だけを再利用対象とし、fixtureで確認できた最大は100×200=20,000要素。
  - 実測対象の`System.Int64[,]` allocation rateは約92.7%削減、`PrivateInt2DVariableToken.ScopeIn`のsampled allocationは0でした。
  - Release buildは0 errors / 35 warnings、既存セーブを含む通常プレイsmokeは異常なしでした。
  - 配布EXEは24,639,210 bytes、SHA-256は`490A18439767811468D930D8D8E3A69DFEA5DDD7B7D904A65A47B04A8B8DAE33`です。
- 詳細は`プロジェクト資料/修正履歴/#今回の修正説明_2026-08-19.md`を参照してください。
- Phase 12Cとして、HTML表示のstrict color-only `FONT` fast pathをProductionへ採用しました。
  - 完全入力を先に検証し、通常文字列と厳密な`<font color = '#RRGGBB'>text</font>`だけを既存表示オブジェクトへ直接変換します。
  - その他のHTML、属性、entity、制御文字、大小文字違いなどは従来どおりAngleSharpへfallbackします。
  - C2/C3/C5検証で適格入力7,938/7,938、formal smoke process exit 0、同一入力steady測定で対象経路のallocation約80.839%削減（時間約87.346%削減）を確認しました。値は対象経路限定です。
  - Release buildは0 errors / 35 warnings、新規warning 0。配布EXEは24,643,306 bytes、SHA-256は`4CE75612E0A370DC6FFDD80F369E0E50F9F31F0CD81AA732F35F4FDBBFD6B271`です。

### 2026-08-17
- 性能改善後のrepository cleanup / code cleanupをProductionへ反映しました。
  - 現行資料と調査履歴を分離し、古い検証raw記録や重複資料を整理。
  - 未使用の解析補助コード、Visual Studioの未使用Resources/Settings雛形、caller 0の内部コード、旧HTML解析残骸、空イベントhandler、コメントアウト済み旧実装を削除。
  - 現行AngleSharp解析で使用中の`DivState`など、実行経路が残るコードは維持。
- Phase 3A / 3B / 3CはいずれもRelease build、Normal、口上有りKojoの起動確認を通過しました。caller 0のdead code、未使用のVisual Studio雛形、旧HTML parser残骸が現役経路と混在していたため、段階buildで削除範囲を確認しました。
  - Phase 3Cでは既存warningのユニーク件数が70件から52件へ減少し、新規warningは0件でした。
  - ERB/ERH、セーブデータ形式、ゲーム処理の仕様を変更するcleanupではありません。
- 続けてPhase 4として、挙動を変えずに安全な意味付けができる既存warningだけを選別して整理しました。SkiaSharp obsolete、意図的enum重複、nullable、現役DivStateなどは無理に変更していません。
  - 未使用field、`nameof`化、range表記、Dispose/finalizer処理、private引数型、例外`paramName`を整理。
  - Release buildのwarningは52件から37件へ減少し、新規warningは0件でした。
  - Phase 4A / 4B / 4CはいずれもRelease build、Normal、口上有りKojoの起動確認を通過しました。
- Phase 5として、VARSIZE/macro、ARRAYMSORT、graphics colorで、正常入力の拒否、overflow、bounds/ARGB誤りが起きる既存バグをfocused test付きで修正しました。
  - Phase 5A: `ReadSingleIdentifierWord`のmacro条件、100段macro展開境界、空`#DEFINE`判定を修正。
  - Phase 5B: `ARRAYMSORT`の3D配列判定とInt64比較overflowを修正。
  - Phase 5C: `GGETCOLOR` / `GSETCOLOR`の負Y判定と`SPRITEGETCOLOR`のARGB packingを修正。
  - Release build 0 errors、37 warnings、新規warning 0、focused tests / Normal / 口上有りKojo PASS。
- Phase 6として、無効Sprite入力のNREとClipboard不正設定値による起動例外の既存バグをfocused test付きで修正しました。
  - Phase 6A: `SPRITEANIMEADDFRAME`の無効Sprite入力時NullReferenceExceptionを修正。
  - Phase 6B: ClipboardのBufferSize / MinTimerの0・負値を安全化し、ConfigDialogでも最小値1に制限、既存の不正設定値はruntimeで1へclamp。
  - Release build 0 errors、37 warnings、新規warning 0、focused tests / Normal / 口上有りKojo PASS。
- Phase 7として、Clipboard表示/起動guard、配列サイズoverflow、CircularBuffer stale参照の既存バグをfocused test付きで修正しました。
  - Phase 7A: ClipboardメニューのChecked状態復元と、Ctrl+Up / Ctrl+Downの起動時null guardを追加し、warningを37件から35件へ削減。
  - Phase 7B: VariableSize.csvの2D/3D/CDFLAG要素数計算をInt64 arithmeticへ変更し、巨大配列focused testでOOMなくInputReadyまで到達することを確認。
  - Phase 7C: `CircularBuffer.Clear()`でbacking arrayもclearし、stale value保持を修正。focused test `PHASE7C_PASS`。
  - Release build 0 errors、35 warnings、新規warning 0、Normal / 口上有りKojo PASS。
- Phase 8として、演算子評価と文字列比較の既存バグをfocused test付きで修正しました。
  - Phase 8A: unary minusのoperand二重評価を修正し、副作用関数が1回だけ評価されることを確認。
  - Phase 8B: string `>=` / `<=`の比較条件を修正し、6比較ケースとstrict comparison sanity testを確認。
  - Release build 0 errors、35 warnings、新規warning 0、focused tests / Normal / 口上有りKojo PASS。
- Phase 9として、数値parserの基数・指数表記回帰、PICKUPCHARA後のspecial index取り違え、RAND range overflowの既存バグをfocused test付きで修正しました。
  - TOINT / ISNUMERIC、数値リテラル、MASTER/TARGET/ASSI追随、RANDの最大許容rangeとoverflow診断を確認しました。
  - Release build 0 errors、35 warnings、新規warning 0、focused tests / Normal / 口上有りKojo PASS。
- Phase 10Aとして、POWERがdouble計算を経由することで大きな整数に丸め誤差やInt64境界値の誤判定を生じる問題を修正しました。
  - checked Int64 arithmeticによる整数二乗法へ変更し、式中関数版と旧命令版の処理を共通化しました。
  - Release build 0 errors、35 warnings、新規warning 0、focused tests / Normal / 口上有りKojo PASS。
- Phase 10Bとして、InstructionLineのrare reference統合、関数private variable dictionaryのlazy生成、VoidArgument共有化により、大規模ERB構成の起動後retained memoryを削減しました。
  - Kojo fixtureの3-run中央値で、Phase 10B全体としてmanaged memory約125MiB、working set約127MiBの削減を確認しました（測定環境依存）。
  - Release build 0 errors、35 warnings、新規warning 0、Normal / 口上有りKojo PASS。起動時間の明確な退行は確認されませんでした。
- 詳細は `プロジェクト資料/修正履歴/#今回の修正説明_2026-08-17.md` を参照してください。

### 2026-08-15
- ReadFirstIdentifier Candidate DをProductionへ採用しました。
  - 行頭命令名解析で、`.Code`だけを必要とする経路の一時`IdentifierWord` wrapper生成を削減。
  - Normal allocation約-30.0MB（-1.59%）、Kojo約-138.5MB（-2.44%）。
  - Normal/Kojo互換性PASS。GC countは混在しており改善とは主張しません。
- 詳細は `プロジェクト資料/修正履歴/#今回の修正説明_2026-08-15.md` のReadFirstIdentifier Candidate D Production Adoption節を参照してください。
- ReadString Candidate BをProductionへ採用しました。
  - escape無し文字列では`StringBuilder`生成を回避し、escape有りは従来相当経路へfallback。
  - Normal allocation約-383.2MB（-16.91%）、Kojo約-404.8MB（-6.66%）、GC pressure改善。
  - focused compatibility、Normal/Kojo startup PASS。Startup全体で明確な一貫した性能退行なし。
- 詳細は `プロジェクト資料/修正履歴/#今回の修正説明_2026-08-15.md` のReadString Candidate B Production Adoption節を参照してください。
- CharStream Candidate AをProductionへ採用しました。
  - `ReadEnabledLine`内の一時`CharStream`を`Reset`して再利用（共有pool/cacheなし）。
  - Normal allocation約-71.1MB（-3.04%）、Kojo約-244.1MB（-3.86%）。
  - Normal/Kojo互換性PASS、Startupで明確な性能退行なし。
- 詳細は `プロジェクト資料/修正履歴/#今回の修正説明_2026-08-15.md` のCharStream Candidate A Production Adoption節を参照してください。
- 互換性・Memory・安定化後のRuntime確認を完了したLoopState Candidate BをProductionへ採用しました。
  - 通常`InstructionLine`からloop専用の`LoopEnd` / `LoopCounter` / `LoopStep`領域を分離。
  - FOR / REPEATの開始行だけ`LoopInstructionLine`を使用。
  - Normal/Kojo startup、FOR、REPEAT、再入の最小runtime smokeを確認。
  - 安定化後の正式結果では、P01約+0.14%、P02約-3.44%で、明確な性能退行は確認されませんでした。
- 詳細は `プロジェクト資料/修正履歴/#今回の修正説明_2026-08-15.md` のLoopState Candidate B Production Adoption節を参照してください。
- ERB読み込み時の一時オブジェクト生成を追加で削減しました。
  - `CharStream` の不要な生成を減らし、同一読み込み処理内で安全に再利用するよう変更。
  - 単純な文字列解析では `StringBuilder` などの一時バッファを生成しない経路を追加。
  - 行頭命令名の解析で、直後に破棄されていた `IdentifierWord` の一時生成を省略。
- 大規模なERB構成で、起動完了後も不要なメモリが大量に保持されたように見える状態を改善しました。
  - 起動完了時のmanaged memoryが2 GiB未満なら追加GCを行わず、小規模構成の起動時間を優先。
  - 2 GiB以上の場合のみAggressive / blocking / compacting GCを1回実行し、起動後のWorking Setを整理。
  - 検証用の大量口上構成ではWorking Setを約1.50 GB（36.30%）削減。
  - 同構成では起動完了まで約0.99秒増加するため、メモリ使用量とのトレードオフです。
  - 通常構成ではTrimは発動せず、明確な起動性能退行は確認されませんでした。
- 既存ERB・セーブデータの形式やゲーム処理の仕様は変更していません。
- 詳細は `プロジェクト資料/修正履歴/#今回の修正説明_2026-08-15.md` を参照してください。


### 2026-08-14
- ERB解析処理の内部構造を見直し、起動時のメモリ確保量を削減しました。
  - Argument Parserの一時オブジェクト生成を削減。
  - WordCollectionを通常時は軽量なList形式で保持し、必要な場合のみLinkedListへ変換するよう変更。
  - Lexerのtoken格納Listについて、不要な中間配列生成とコピーを削減。
- 並列ERB解析まわりの安全性を改善しました。
- ERB起動処理を調査するための汎用Profiling機能を整理・追加しました。
- 既存ERBとの互換性を維持したまま、特に大量の口上ファイルを含む構成で起動時allocationを削減しています。
- `InstructionLine` の `Argument` / `argprimitive` 保持領域を1つの参照へ統合し、既存の引数・プリミティブ意味論を維持したまま行オブジェクトを軽量化しました。
- 詳細は `プロジェクト資料/修正履歴/#今回の修正説明_2026-08-14.md` を参照してください。

## 2026-08-13
・起動時に極めて低頻度で、ERBの関数名が別のMacro展開先として誤認され、実際の行とは異なる関数名の引数エラー等が表示されることがある問題を修正しました。Macro識別に文字列のハッシュ値のみを使用していた処理を、文字列そのものを比較する方式へ変更しています。

## 2026-08-16 - Gamepad Key Config v1 / Direct Button Capture（正式採用）

- 「設定→ゲームパッド」タブを追加し、決定、戻る・キャンセル、高速送り、前後ページ、ゲームパッド設定を開く操作、マクロ実行1～3を物理ボタン単位で変更できるようにしました。OPTIONS / Startは初期状態ではゲーム中のActionに割り当てません。
- 全項目を未割り当てにでき、重複時は自動入れ替え、初期設定への復元に対応しています。設定は`setting_user.json`へPS/Xbox共通の物理位置名で保存します。
- 初期配置では□ / Xがゲームパッド設定を開く操作です。D-pad / Left Stickは従来どおり固定操作です。
- Binding行でConfirmを押した後、物理ボタンを直接割り当てるCaptureに対応しました。開始ボタンの誤登録防止、複数押し拒否、D-pad左取消、既存duplicate swapを維持しています。
- Capture中は対象Binding欄へ「ボタンを押してください...」をOverlay表示し、設定完了後または取消後に通常表示へ戻ります。正式配布版へ反映しました。

## 2026-08-16 - 最新mainへのController/Gamepad正式統合

- 最新mainのERB起動高速化、allocation削減、Parser最適化、条件付き起動後メモリ整理を維持したまま、Controller/Gamepad対応を統合しました。
- XInput、WinMM joystick API、Raw Input/HIDを使い、Xbox系・PS4系ゲームパッドを自動認識します。
- D-pad / Left StickによるUI・ゲーム操作、× / A決定、○ / B戻る・キャンセル、LB / RBページ移動に対応しています。OPTIONS / Startは残りの任意Actionへ割り当て可能です。
- Virtual Gamepad Focus、HTML / INPUTMOUSEKEY、Modal popup、Alt+Tab後の復帰、画面遷移後のFocus復帰、複数行Buttonの論理Target統合を維持しています。
- △ / YはESC / マウス右クリック相当の高速送りです。会話や戦闘中の表示・待機を処理し、次の入力が必要な地点まで進みます。□ / Xはゲームパッド設定を開く初期割り当てで、設定画面から変更できます。
- Release Publish単一EXEを24,757,994 bytes（SHA-256: `FA2184F0B5B2518A75DA462F309EDACBE49413D627E514224B0883D2289E77BB`）へ更新しました。正式統合コミット: `d2406bda44f3ae04c5a96639f62bb70c39170188`。

### 2026-08-15
- ReadFirstIdentifier Candidate DをProductionへ採用しました。
  - 行頭命令名解析で、`.Code`だけを必要とする経路の一時`IdentifierWord` wrapper生成を削減。
  - Normal allocation約-30.0MB（-1.59%）、Kojo約-138.5MB（-2.44%）。
  - Normal/Kojo互換性PASS。GC countは混在しており改善とは主張しません。
- 詳細は `#今回の修正説明_2026-08-14.md` のReadFirstIdentifier Candidate D Production Adoption節を参照してください。
- ReadString Candidate BをProductionへ採用しました。
  - escape無し文字列では`StringBuilder`生成を回避し、escape有りは従来相当経路へfallback。
  - Normal allocation約-383.2MB（-16.91%）、Kojo約-404.8MB（-6.66%）、GC pressure改善。
  - focused compatibility、Normal/Kojo startup PASS。Startup全体で明確な一貫した性能退行なし。
- 詳細は `#今回の修正説明_2026-08-14.md` のReadString Candidate B Production Adoption節を参照してください。
- CharStream Candidate AをProductionへ採用しました。
  - `ReadEnabledLine`内の一時`CharStream`を`Reset`して再利用（共有pool/cacheなし）。
  - Normal allocation約-71.1MB（-3.04%）、Kojo約-244.1MB（-3.86%）。
  - Normal/Kojo互換性PASS、Startupで明確な性能退行なし。
- 詳細は `#今回の修正説明_2026-08-14.md` のCharStream Candidate A Production Adoption節を参照してください。
- 互換性・Memory・安定化後のRuntime確認を完了したLoopState Candidate BをProductionへ採用しました。
  - 通常`InstructionLine`からloop専用の`LoopEnd` / `LoopCounter` / `LoopStep`領域を分離。
  - FOR / REPEATの開始行だけ`LoopInstructionLine`を使用。
  - Normal/Kojo startup、FOR、REPEAT、再入の最小runtime smokeを確認。
  - 安定化後の正式結果では、P01約+0.14%、P02約-3.44%で、明確な性能退行は確認されませんでした。
- 詳細は `#今回の修正説明_2026-08-14.md` のLoopState Candidate B Production Adoption節を参照してください。
- ERB読み込み時の一時オブジェクト生成を追加で削減しました。
  - `CharStream` の不要な生成を減らし、同一読み込み処理内で安全に再利用するよう変更。
  - 単純な文字列解析では `StringBuilder` などの一時バッファを生成しない経路を追加。
  - 行頭命令名の解析で、直後に破棄されていた `IdentifierWord` の一時生成を省略。
- 大規模なERB構成で、起動完了後も不要なメモリが大量に保持されたように見える状態を改善しました。
  - 起動完了時のmanaged memoryが2 GiB未満なら追加GCを行わず、小規模構成の起動時間を優先。
  - 2 GiB以上の場合のみAggressive / blocking / compacting GCを1回実行し、起動後のWorking Setを整理。
  - 検証用の大量口上構成ではWorking Setを約1.50 GB（36.30%）削減。
  - 同構成では起動完了まで約0.99秒増加するため、メモリ使用量とのトレードオフです。
  - 通常構成ではTrimは発動せず、明確な起動性能退行は確認されませんでした。
- 既存ERB・セーブデータの形式やゲーム処理の仕様は変更していません。
- 詳細は `#今回の修正説明_2026-08-15.md` を参照してください。

- Candidate Kの条件付き起動後メモリ整理をProductionへ採用しました。
  - 起動完了時のmanaged memoryが2GiB未満なら`MemoryTrimSkipped`として強制GCを行いません。
  - 2GiB以上の大規模構成だけ、Aggressive / blocking / compacting full GCを1回実行します。explicit LOH `CompactOnce`は使用しません。
  - NormalではTrimが発動せず、今回の大規模Kojo fixtureではWorking Set約-1.502GB（-36.30%）、Managed約-987MB（-30.46%）を確認しました。
  - Kojo InputReadyは約+0.99秒、trim medianは約1.036秒であり、小規模構成は速度優先、大規模構成は起動時tradeoffと引き換えに起動後メモリを整理する仕様です。
- 詳細は `プロジェクト資料/08_ArgumentParser性能設計.md` と `#今回の修正説明_2026-08-14.md` のCandidate K節を参照してください。


### 2026-08-14
- ERB解析処理の内部構造を見直し、起動時のメモリ確保量を削減しました。
  - Argument Parserの一時オブジェクト生成を削減。
  - WordCollectionを通常時は軽量なList形式で保持し、必要な場合のみLinkedListへ変換するよう変更。
  - Lexerのtoken格納Listについて、不要な中間配列生成とコピーを削減。
- 並列ERB解析まわりの安全性を改善しました。
- ERB起動処理を調査するための汎用Profiling機能を整理・追加しました。
- 既存ERBとの互換性を維持したまま、特に大量の口上ファイルを含む構成で起動時allocationを削減しています。
- `InstructionLine` の `Argument` / `argprimitive` 保持領域を1つの参照へ統合し、既存の引数・プリミティブ意味論を維持したまま行オブジェクトを軽量化しました。
- 詳細は `#今回の修正説明_2026-08-14.md` を参照してください。

## 2026-08-13
・起動時に極めて低頻度で、ERBの関数名が別のMacro展開先として誤認され、実際の行とは異なる関数名の引数エラー等が表示されることがある問題を修正しました。Macro識別に文字列のハッシュ値のみを使用していた処理を、文字列そのものを比較する方式へ変更しています。

## 2026-07-31 - .NET 10対応版の確認と配布更新

### 基盤・表示

- 対象フレームワークを.NET 10へ更新しました。
- Windows 11のダークモード対応を取り込みました。
- SkiaSharp 4.150.1を使用する構成に更新しました。
- OpenGLの垂直同期設定を、現在のOpenTKに合う方法へ移しました。
- Windows API全体の.NET投影DLLは同梱せず、SkiaSharpが必要とする`WinRT.Runtime.dll`だけを単一EXEへ含める構成にしました。
- 配布EXEを49,518,008バイト（約47.2 MiB）から24,639,210バイト（約23.5 MiB）へ削減しました。

### ログ表示

- CRER氏による`ClipBoardDialog`の高速化を取り込みました。
- 大量ログの設定中だけ途中再描画を止め、ログ内容を省略せずに表示を高速化しました。
- ログ表示欄を大量文字列に向く`RichTextBox`へ変更しました。
- Ctrl+Cとメニューの両方で、閉じたログ画面と保持中のログを確実に破棄するようにしました。

### 安全性・依存パッケージ

- SQLiteのネイティブライブラリを含むパッケージを2.1.12へ固定し、既知の脆弱性が報告された古い版を使わないようにしました。
- System.CommandLineをプレビュー版から安定版2.0.10へ更新し、呼び出しコードも安定版APIに合わせました。
- AngleSharp 1.6.0を使用する更新を取り込みました。
- `Microsoft.Windows.CsWinRT` 2.2.0を明示参照し、従来の大きなWindows SDK部品から使われていたものと同じ`WinRT.Runtime.dll`を保持しました。

### 説明・配布物

- 動作条件と現在の機能に合わせて、ルートのREADMEを更新しました。
- `配布/Emuera.NET_最終通常版/Emuera.exe`を、必要なWinRT部品だけを含む.NET 10対応版へ更新しました。
- 採用済みソースだけを収めるZIP、配布README、SHA-256一覧を現在の内容に合わせて更新しました。

### 確認結果

- Releaseビルド: 成功（エラー0件）
- NuGet既知脆弱性検査: 検出0件
- NuGet非推奨パッケージ検査: 検出0件
- 単一EXE内部: `WinRT.Runtime.dll`あり、`Microsoft.Windows.SDK.NET.dll`なし
- eramegatenP: 利用者による手動起動・操作確認で問題なし
- ログ表示: 利用者がCtrl+C・メニュー経路を手動確認し、問題なし

## 2026-07-15 - 最終通常版として採用した既存改修

- 起動時にまれに発生していた、ERBのラベルや関数を未定義と誤判定する警告を抑止しました。
- ERBの解析処理を中心に起動時間を短縮しました。
- マクロ中のゲーム処理と出力内容を維持しつつ、画面更新をまとめてマクロ実行を高速化しました。
- マウスの「戻る」「進む」サイドボタンから、画面内の前ページ・次ページ操作を行えるようにしました。
- 既存のセーブデータ、ERB、ERH、CSV、設定ファイルを変更せずに利用できる方針を維持しました。
