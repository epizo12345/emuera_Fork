# 今回の修正説明（2026-08-11 ゲームパッド対応 v1）

この文書は、ゲームパッド対応v1で変更したコード、設計理由、検証状態をGitレビュー用にまとめたものです。ここでいうv1はEmuera本体の製品バージョンではなく、ゲームパッド機能の最初のまとまった仕様版です。

## 最初に結論

Emueraの既存入力処理を置き換えず、その手前に次の3層を追加しました。

1. Windowsからゲームパッド状態を取得する入力層
2. 現在画面の選択可能要素から上下左右関係を作るNavigation層
3. 選択中の要素を既存のクリック・決定・キャンセル処理へ渡す実行層

ゲーム側ERB、CSV、セーブ形式は変更していません。外部入力変換ツールも必須ではありません。

## 1. 入力取得層

中心は`UI/Game/GamepadInput.cs`です。XInput #0～#3、WinMM、Raw Inputの順で使用します。

WinMMでは`joyGetNumDevs()`を接続台数として扱わず、各indexについて`joyGetDevCaps`と`joyGetPosEx`を実行します。実機で`Microsoft PC ジョイスティック ドライバー`という名前のPS4互換パッドが正常入力を返したため、名前による除外は廃止しました。

`UI/Game/RawInputGamepad.cs`は、WinMMで取得できないHIDゲームパッド向けのフォールバックです。高度なHID機能は扱わず、v1で必要な方向、基本ボタン、肩ボタン、Startに限定しています。

### レビュー反映: Raw Inputの方向ソース

Raw Inputでは以前、HatがないパッドのX/Y軸を方向として取得した際に、その方向を十字キー相当の1値へ混在させていました。そのため左スティックであっても`DirectionSource.DPad`になり、既存の`TryGamepadDirectInput()`へ届きませんでした。

現在は`RawInputGamepadSample`に`DPadDirection`と`LeftStickDirection`を別々に保持します。Hat/POVだけがD-pad、X/Y軸だけがLeftStickです。Deadzone、ニュートラル復帰、edge detection、リピートは既存の`GamepadManager.ProcessSample`へ集約したため、XInput/WinMMと同じ処理を使います。

### レビュー反映: デバイス単位のFace Button Layout

PS4系Raw Input候補がPC上にあることだけを根拠に、選択中のWinMMパッドまでPS4配列にするグローバル判定を廃止しました。XInputは常にXbox配列です。WinMMでPS4配列を選ぶのは、WinMM自身の名前・Registry/OEM情報がPS4系を示す場合、またはVID/PID・固有名・一意なWinMM/Raw Inputの組み合わせで同一性を確認できる場合だけです。

実機で使われる`Microsoft PC ジョイスティック ドライバー`（英語名`Microsoft PC Joystick Driver`も同様）はデバイスパスを持たないため、WinMM有効候補1台、Raw Input候補1台、PS4系Raw Input、かつ6軸・14ボタン以上という既知の組み合わせに限ってPS4配列と扱います。複数候補など曖昧な場合は安全側のXbox配列を維持します。その場合は`--GamepadLayout PlayStationWinMM`または`EMUERA_GAMEPAD_LAYOUT=PlayStationWinMM`で明示できます。

## 2. UI Navigation層

`UI/Game/GamepadFocusTarget.cs`が1つの選択対象を表し、`UI/Game/GamepadNavigationGraph.cs`が上下左右リンクを作ります。

`UI/Game/EmueraConsole.cs`は、通常Console、HTML、HTML Island、INPUTMOUSEKEYから現在のInputRequestに属するTargetを収集します。通常行はLineNo、HTMLは実描画Boundsを使用します。画面ごとのゲーム名や入力番号では判定していません。

縦移動は同じXレーンを優先し、隣の行に別列しかない場合はさらに先の行を調べます。これにより調教画面で中央列から左列へ漏れる問題を修正しました。

### 非インタラクティブな通常Console説明文の除外（今回の保守修正）

通常ConsoleのFocus Target収集が、`ConsoleButtonString`の子ノードを表示矩形ごとにたどり、親の選択状態を説明用テキストへ引き継いでいました。そのため、選択肢の下にある説明文・装飾文までボタンのFocus範囲へ混入し、0番から次の選択肢へ移るのに説明行の分だけ移動が必要になる場合がありました。

通常Consoleでは、Emueraが実際のクリック・決定対象として生成した`ConsoleButtonString.IsButton=true`のオブジェクトを1つのTargetとして収集し、子の`ConsoleStyledString`や`IsButton=false`の通常表示要素は独立したTargetにしないようにしました。これにより、説明文を挟む`[0] → [1]`はDown 1回で移動します。

判定に入力値は使いません。`Input=0`でも`IsButton=true`なら正常な選択肢として残し、通常テキストに内部既定値の0相当があっても`IsButton=false`なら除外します。HTML_PRINT、HTML Island、INPUTMOUSEKEYは従来のクリック可能要素収集を維持し、HTMLボタンと通常HTMLテキストを区別します。

### 論理ボタン断片の統合（今回の追加修正）

実機の`gamepad-debug.log`では、前回の修正後も説明行がFocus Targetとして残りました。原因は、それらが`IsButton=false`の説明文ではなく、表示の折り返し・分割処理で生成された別々の`ConsoleButtonString`であり、各断片に`IsButton=true`と同じ入力値が付いていたためです。したがって、単純に非ボタンを除外するだけでは解決しません。

収集時の同一オブジェクト統合は既存の`byButton`で維持し、その後の通常Consoleだけに限定した保守的な統合段階で、別インスタンスの断片を確認します。統合条件は次の全てです。

- 同じ`SourceType`、Console layout、Base/Source group
- 同じ`ConsoleButtonString.Generation`と`Input/Inputs`（InputRequestは現在画面のものだけ）
- 連続または近接した`LineNo`とY位置
- 近いX位置、幅の類似または水平方向の重なり

同じ入力番号でも遠い場所・別列・別領域にあるTargetは統合しません。特に水平に並ぶ同番号ボタンは、後続断片のYが進まない条件で分離します。HTML/HTML IslandはDOM/Island側の既存単位を維持し、今回のヒューリスティックを適用していません。

統合後の代表はブロックの最初の断片です。代表の`Button`は変更せず、`Bounds`と`RawBounds`だけを全断片のUnionにするため、ゲームパッドのハイライト範囲はブロック全体になり、×決定・○semantic Back・INPUTMOUSEKEYの仮想クリックは元のConsoleButtonStringの入力値を既存共通経路へ渡します。Navigation Graphには論理ボタン1つだけが入り、0番の4断片から1番の先頭へDown 1回で移動します。

`--GamepadDebug`では、例えば次の形式を出します。

```text
Logical button group: input=0 fragments=4 representativeLine=201 lines=201,202,203,204
```

自己テストでは、0/1/2について「入力ヘッダー1行＋説明断片3行」を再現し、論理Target数3、0→1→2の上下リンク、代表断片保持を検証します。また、遠い同一Input=5が2Targetのまま残ることも確認します。

### レビュー反映: Row skip診断

`FindVerticalCandidate`は、同じレーンの候補が見つかるまで複数の視覚行を検索する設計です。診断側だけが「上下は必ずRow±1」と判定していたため、正常な`65 → 79`のようなリンクを誤警告していました。診断もDownは`target.Row > source.Row`、Upは`target.Row < source.Row`だけを確認します。通常Consoleでは別の構造行が同じ描画Boundsを共有することがあるため、描画Yを追加条件にはしません。逆方向リンクは引き続き検出します。

## 3. 決定・キャンセル層

×と○で別々の簡易入力を作らず、選ばれた`GamepadFocusTarget`を共通の実行経路へ渡します。

- 通常ボタン: その`ConsoleButtonString`を通常決定した場合と同じ処理
- INPUTMOUSEKEY: OSカーソルを動かさない仮想左クリック
- semantic Back: 現在InputRequest内のCANCEL/BACKを探し、同じ共通決定処理
- Back候補なし: 既存のEsc・右クリック相当

全角の`ＣＡＮＣＥＬ`が通常の文字列比較で認識されなかったため、比較専用文字列へ`NormalizationForm.FormKC`を適用しています。表示文字列は変更しません。

### ON/OFF設定画面のフォーカス維持（今回の保守修正）

ON/OFF切替のように、×決定後にERBが同じ画面を再描画し、新しい`InputRequest`と`ConsoleButtonString`を生成する画面では、従来のRequest IDだけのフォーカス履歴を使えませんでした。その結果、再構築されたNavigation Graphの先頭項目へフォーカスが戻っていました。

`GamepadConfirm()`が決定を実行する直前に、現在のTargetと画面内Target一覧を短命な`PostConfirmFocusAnchor`へ保存します。次に`GamepadEnsureSelection()`がGraphを再構築したとき、次の順で安全性を確認します。

1. 対象数が大きく変わっていないこと
2. 入力値、Source/Base group、Back属性、表示Boundsが一致するTargetが多数あること
3. 保存した入力値のTargetが同じSource/Base groupかつ近いBoundsに存在すること

表示色、選択ハイライト、スタイルは比較材料にしません。対象が見つからない場合だけ、同じSource/Base groupとBack属性を保った縦レーンの下方向、上方向、近傍の順でフォールバックします。画面構造が変わった場合は復元を拒否し、通常の初期フォーカス選択へ進みます。

別画面の同じ入力番号を誤って復元しないよう、PostConfirm Anchorは×決定後だけ有効で、現在の画面構造の一致を必須にしています。`input=0`などの入力値単独、旧InputRequestの履歴、色やハイライトによる判定は使いません。○キャンセルはAnchorを作成せず、既存のsemantic Back検索と共通決定経路を維持します。

`--GamepadDebug`では次の診断を出します。

```text
Confirm focus anchor: ...
Next request: ...
Post-confirm redraw comparison: same-screen=... target-match=...
Focus restored: ...
```

構造が変わった場合は`Post-confirm focus restore rejected: reason=UI structure changed`、対象が0件の場合はその理由も記録します。

## 4. MainWindow統合と起動回帰

ゲームパッド追加途中では、`MainWindow`コンストラクタでGamepadManagerとTimerを開始し、初回表示前のHandle作成時にRaw Inputを登録していました。ゲームパッド追加前のGit版にはこの処理がなく、起動時の白い上・右・下領域はこの追加経路で発生した回帰でした。

v1では元の起動経路を戻し、`console.Initialize()`と`CompleteStartup()`の完了後に`BeginInvoke`でゲームパッド初期化を開始します。前回試したOpacityによる隠蔽は撤回し、コードから削除しました。

## 5. 診断オプション

`Program.cs`へ次を追加しました。

```powershell
Emuera.exe --GamepadDebug
Emuera.exe --GamepadDirectInput Auto
Emuera.exe --GamepadLayout Auto
```

`--GamepadDebug`はデバイス検出だけでなく、Navigation Graph、移動、決定、キャンセル候補、実行経路、Alt+Tab復帰を記録します。値が変わった場合だけ状態を記録し、毎フレーム同じ内容を大量出力しません。

`--GamepadLayout`は非XInputデバイスだけのFace Button Layout overrideです。`Auto`（既定）、`Xbox`、`PlayStationWinMM`を指定できます。XInputはこの指定にかかわらずXbox配列です。接続時のログにはBackend、JoyId/VID/PID、FaceButtonLayout、選択理由を記録します。

## 6. 変更ファイル

### 既存ファイル

- `Program.cs`: 診断・直接入力プロファイル・Face Button Layout overrideの起動オプション
- `UI/Framework/Forms/MainWindow.cs`: 初回表示後の入力層初期化、Timer、Raw Inputメッセージ、Alt+Tab復帰、UI操作への橋渡し
- `UI/Game/ConsoleDisplayLine.cs`: HTML由来表示行の識別
- `UI/Game/EmueraConsole.Print.cs`: 表示更新時のNavigation Graph再構築フラグ
- `UI/Game/EmueraConsole.cs`: Interactive Target収集、フォーカス、移動、決定、キャンセル、直接入力、診断

### 追加ファイル

- `UI/Game/GamepadFocusTarget.cs`: 共通フォーカス対象
- `UI/Game/GamepadInput.cs`: XInput / WinMM入力、デバイス単位のボタン配列、論理アクション
- `UI/Game/GamepadNavigationGraph.cs`: 行・列・上下左右リンクとRow skip検証
- `UI/Game/RawInputGamepad.cs`: D-pad/左スティックを分離したRaw Input / HIDフォールバック

## 7. 検証状態

- `dotnet build -c Release --no-restore`: 成功、エラー0件（既存のSDK/静的解析警告79件は残存）
- Release単一EXE: 24,721,130 bytes
- SHA-256: `AD88C344970D4EAD5114854309E4890742A4BE17BD948ED67BC50F59FBB58543`
- 隔離データのStartupTestMode: 2/2成功、ExitCode 0、警告Lv2 0件（平均起動2,910ms、平均ERB読込1,425ms）
- PS4互換パッドのWinMM認識: 利用者の実機ログで確認済み
- ×決定、Navigation、全角CANCELの○操作: 利用者の実機操作で確認済み
- 最新の起動ライフサイクル復元後の瞬間表示: 利用者による再確認待ち
- `--GamepadDebug`時の自己テスト: Raw D-pad/LeftStickの方向ソース、デバイス単位の配列判定、Interactive TargetのA/B/C/D、論理ボタン断片の0/1/2ブロック・リンク・代表保持・遠隔同一Input非統合、Navigation Graphのlist/grid/keypad、独立縦レーン/Row skip検証、PostConfirmフォーカス維持のA/B/C/DがPASS。隔離実画面のGraphはtargets=49、warnings=0。

## 8. Gitへ含めるもの

Gitには上記の既存5ファイル、追加4ファイル、仕様書、修正説明、更新履歴を含めます。

`GamepadBuild/`、`GamepadBuildSingleFile/`、`GamepadHandoff_*.zip`、`gamepad-debug.log`、`artifacts/`は生成物または診断物なので、通常のソースコミットには含めません。Gitで追跡するv1配布物は`配布/Emuera.NET_ゲームパッド対応_v1/`の単一EXE、README、SHA-256一覧に限定します。

## 9. レビュー時の確認順

1. `プロジェクト資料/07_ゲームパッド対応_v1_仕様書.md`
2. `UI/Game/GamepadInput.cs`
3. `UI/Game/RawInputGamepad.cs`
4. `UI/Game/GamepadFocusTarget.cs`
5. `UI/Game/GamepadNavigationGraph.cs`
6. `UI/Game/EmueraConsole.cs`の`GAMEPAD-V1`コメント
7. `UI/Framework/Forms/MainWindow.cs`の遅延初期化
8. `Program.cs`の起動オプション
