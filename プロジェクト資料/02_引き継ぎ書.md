# Emuera.NET 高速・互換版 引き継ぎ書

最終更新: 2026-08-22
引き継ぎ時点: Phase 13R37 LogicalLine ScriptPosition compact storageのProduction採用・配布更新・pushまで完了

## 1. 何のプロジェクトか

Emuera.NET `BugFix_Test` を基礎に、個人利用中の大規模ゲーム `eramegaten_p` を安全に動かしながら、次を改善する派生プロジェクトである。

1. 並列ERB解析でまれに出る偽の起動警告をなくす
2. EXE起動から操作可能になるまでを短縮する
3. 大量入力マクロの実行時間を短縮する
4. マウスの戻る・進むボタンでゲーム内の前後ページを選べるようにする
5. Ctrl+Cまたはメニューから大量ログを開く待ち時間と、閉じた後の不要な保持を減らす

速度優先だが、セーブ、ERB評価、乱数、入力順、待機命令、最終状態の後方互換性が上位条件である。

## 2. 基礎と配置

- ワークスペース: この文書の親プロジェクト
- 基礎コミット: `7b7dd3bf240eff4fdfc7094f4175de0e014532b7`
- 基礎ブランチ名: `BugFix_Test`
- 固定した参照原本: `プロジェクト資料/参照原本/EmueraDotNet-BugFix_Test_7b7dd3bf.zip`
- 最終通常版: `配布/Emuera.NET_最終通常版/Emuera.exe`
- 適用用差分: Gitのmain履歴（固定原本との差分はGitで確認する）
- テストゲーム: `eramegaten_p/`
- テストセーブ: `eramegaten_p/Data/sav/save219.sav`

プロジェクト所有者はGitで履歴と配布元を管理する。AIや引き継いだ実装者は、明示依頼なしにstage、commit、pushを行わない。比較版が必要な場合は名前付きフォルダへ分離し、採否確定後に暫定版を削除する。

## 3. 今の状態

- Phase 12B1.1をProductionへ採用し、`PrivateInt2DVariableToken`のbounded reuse（32,768要素以下）で対象allocation rate約92.7%削減を確認済み
- Phase 12B1.1採用時の配布EXEは24,639,210 bytes、SHA-256は`490A18439767811468D930D8D8E3A69DFEA5DDD7B7D904A65A47B04A8B8DAE33`
- 既存セーブを含む通常プレイsmokeはPASS
- Phase 12C strict color-only FONT fast pathをProductionへ採用。対象外HTMLは従来のAngleSharpへfallbackする
- C2 v2は7,938/7,938、C3 smokeは約352.319秒exit 0、C5は5,538/5,538 equivalent、対象経路allocation約80.839%削減
- 配布EXEは24,643,306 bytes、SHA-256は`67AD3118932E09EDDABDDB48AE7E0FE0D6F59451FDB91C056A4393553FB6388A`
- Phase 13R37で`LogicalLine`内部の`ScriptPosition`をfileId/lineNoへ圧縮し、外部位置情報、reload、raw source、save互換性を維持した。`InstructionLine` shallow sizeは80 bytesから72 bytesへ実測削減し、R36→R37 heap censusでmanaged liveは`1,788,108,159`から`1,758,001,132 bytes`へ変化した。save219後RepeatCount=100 smoke、Release build、startupをPASSした。
- Phase 13R36で`InstructionLine`の`FunctionIdentifier`専用参照slotを削除し、`FunctionCode`とassignment `OperatorCode`をpackした。built-in lookup、SET、method-as-instruction identity、lazy parsing、save互換性を維持し、shallow sizeは88 bytesから80 bytesへ実測削減した。R35→R36同一InputReady censusでmanaged liveは`1,819,500,051`から`1,786,073,667 bytes`へ変化し、save219後RepeatCount=100 smoke、Release build、startupをPASSした。
- Phase 13R35で`InstructionLine`だけerror messageをR34のargument storageへunionし、InstructionLine shallow sizeを96 bytesから88 bytesへ実測削減した。R34→R35同一InputReady heap censusでmanaged liveは`1,851,447,905`から`1,818,291,905 bytes`へ変化し、CharStream 0件を維持した。save219後RepeatCount=100 smoke、Release build、startupをPASSした。
- Phase 13R34でlazy argumentのraw `CharStream`をsource/offset snapshotへ変更し、reader単位のstream再利用を追加した。InputReady heap censusで`CharStream`は`1,960,514 objects / 62,736,448 bytes`から0件、managed liveは`1,911,258,161`から`1,848,434,565 bytes`へ変化した。`InstructionLine`は96 bytesを維持し、save219後RepeatCount=100 smoke、Release build、startupをPASSした。配布EXEは24,643,306 bytes、SHA-256は`84A999C7A6336F46DB6655368A482347D7A78CEBBA349F125196EB8BA7969222`。
- Phase 13R32で`LogicalLine`の`isError`と`errMes`を1slotへ統合した。正常行の`ErrMes`空文字、lazy parser、InvalidLine/InvalidLabelLine、CALL error伝播、save互換性を維持し、InstructionLine shallow sizeは104 bytesから96 bytesへ実測削減した。managed live総量のR32単独差分は測定状態不一致のため未確定。
- Phase 13R31で`LogicalLine`内部のnullable`ScriptPosition` slotを`Filename == null` sentinelへ置換した。公開`Position`、明示的default位置、label/error/reload、save互換性は維持し、InstructionLine shallow sizeは112 bytesから104 bytesへ実測削減した。managed live総量のR31単独差分は測定状態不一致のため未確定。
- Phase 13R30で`InstructionLine.assigndest`専用slotを削除し、SET解析前の左辺`WordCollection`を既存`auxiliaryData`へ一時保持した。retained censusでInstructionLine shallow sizeは120 bytesから112 bytesへ減少した
- Phase 13R29で`ExpressionParser.TermStack.Add(long)`を`SingleLongTerm.FromValue`へ変更し、R25の-1〜255 cacheをparsed ASTへ適用した。retained censusではmanaged live heap約63.2MB、`SingleLongTerm`約64.0MBを削減した
- Phase 13R14で`ExpressionParser.TermStack`のinline-one-element storageをProductionへ採用。大規模口上fixtureのScriptParse中の一時allocationを約251.4MB / 9.60%削減。Server / Workstationの速度はR13反復測定で実質neutral、Workstation GC policyは変更していない

- 最終通常版のソースはワークスペース直下にある
- 最終EXEは発行済み
- 採用した主要機能は実装済み
- 実変更30ファイルには `[Emuera改修:分類番号]` の初心者向け説明コメントを追加済み
- `06_コード案内.md` から機能別にコメントと実装を辿れる
- 起動3回、警告安定性20回、マクロN10/N1000/N5000の記録を恒久配置へ移動済み
- ERBキャッシュ、Candidate E/F/H/I/J等の別GC方式、17～29番の追加最適化は不採用
- 一時ビルド、暫定版、プロファイル、生の作業ログは整理対象から除去済み
- 通常版では性能計測は無効、`EnableStartupMemoryTrim` は既定値true。起動完了時managed memoryが2 GiB未満ならskipし、2 GiB以上だけAggressive / blocking / compacting GCを1回実行する
- サイドボタンの既定候補へ調教画面の `1007` / `1009` とショップ画面の `PREV` / `NEXT` を追加済み
- ショップの `PREV` / `NEXT` は実画面で動作確認済み
- 調教画面はボタンがHTMLの `div` 内に入れ子で保存されるため、通常行・入れ子HTML・HTML Islandを再帰検索するよう修正済み
- 調教画面の内部入力値 `1007` / `1009` が選ばれることを診断ログと実画面で確認済み
- `eramegaten_p/Data/setting_user.json` は同じ候補を明記した複数行JSON。日本語は文字コード誤判定を避けるため `\u....` 形式で保持する
- 診断版でショップ・調教画面の両方を手動検収済み。正式版は同じ検索処理から診断ログ出力だけを除去したもの
- 診断ログ除去後の正式版は起動1回、操作受付到達、警告Lv2なしを確認済み
- CPU・割り当てプロファイルから、`MATCH` / `CMATCH` と `VARSET` / `CVARSET` の使い捨て一時参照をホットパスと特定
- 添字確定済み変数参照をスレッド内・型別・貸出中専有で再利用する `MACRO-04` / `MACRO-05` を採用済み
- 今回のN1000は 57,951.851 ms → 49,767.245 ms。実測・ERB実行回数補正後とも約14.12%短縮
- N100を各3回測定し、ERB 1回あたり約4.27%短縮、一時割り当て約18.40%削減
- 再発行した通常版は起動3/3成功、警告Lv2 0件。N10も完了して最終画面を画像確認済み
- 最終計測版のN5000は309,697.319 ms、`Responding=True` で完走
- CPUプロファイルで `StrForm.GetString` と文字列連結が残る主要ホットパスだと確認
- 埋め込み式が1個だけの整形文字列を直接連結する `MACRO-06` を採用済み。式の評価位置・回数は不変
- 直前の最終版とのN1000比較は 54,406.757 ms → 52,678.744 ms、総時間約3.18%短縮
- N100各3回では総時間約3.62%短縮、文字列生成1回あたり約20.27%短縮、割り当て/ERB約5.79%削減
- `MACRO-06` のN5000は309,139.907 ms、ERB 53,305回・文字列生成203,364,028回を処理して `Responding=True`
- 計測なし最終版は起動3/3成功・警告Lv2 0件。N10も完走し、最終画面を画像確認済み
- .NET 10、Windows 11向けダークモード、SkiaSharp 4.150.1を取り込み済み
- SQLiteとSystem.CommandLineの依存関係を安全な安定版へ更新し、NuGet脆弱性・非推奨検査は検出0件
- 大きな`Microsoft.Windows.SDK.NET.dll`を外し、必要な`WinRT.Runtime.dll`だけを単一EXEへ格納
- 配布EXEは49,518,008バイトから24,639,210バイトへ削減し、利用者がeramegatenPで問題なしと確認済み
- CRER氏による`ClipBoardDialog`高速化を取り込み、表示欄の`RichTextBox`化とログ設定中の再描画抑制を採用済み
- Ctrl+C経路だけでなく、メニューの「ログをクリップボードにコピー」経路にも閉じたダイアログの破棄を追加済み
- ログ内容を省略しないこと、Ctrl+C・メニューの両経路が動くことを利用者が手動確認済み

## 4. 次にやること

次の最優先課題は、起動後約2.3GB、通常プレイで約3.3GBまで上昇してplateauするmemoryについて、allocation量ではなくlive / retained / committedの内訳を直接profilingすることである。Workstation GCは正式採用していない。

次の順序で検収と保留作業を進める。

1. 最終通常版を実ゲームで数日使い、起動警告、ページ操作、マクロ中断、再帰関数、長時間稼働の不具合を収集する
2. 再現する問題があれば、最終通常版フォルダを基準として別フォルダに検証版を作る
3. 無改造 `BugFix_Test 7b7dd3bf` と最終通常版のN1000比較が必要なら、セーブ読込完了を外部から確実に判定する方法を整えて再測定する
4. さらなる高速化は、strict fallback条件と既存AngleSharp互換性を壊さない範囲で、別途プロファイル根拠を確認してから検討する

「何となく速そう」な変更は追加しない。現在版は安定性を含む実用上の基準点である。

## 5. 最短の再開手順

### 通常版をビルド

```powershell
dotnet publish .\Emuera.csproj -c Release -r win-x64 --self-contained false -p:PublishSingleFile=true -o .\配布\再発行確認
```

必要ランタイムは .NET 10 Desktop Runtime x64。SDKはビルドPCだけに必要で、プレイヤーPCには不要である。

### 起動試験

```powershell
& '.\Emuera以外(開発補助ツールなど)\起動安定性テスト.ps1' -ExePath '.\配布\再発行確認\Emuera.exe' -Iterations 3 -TimeoutSeconds 90
```

GUI起動の許可ダイアログが出る実行環境では、テスト開始前に許可する。許可待ちはEmueraのハングではない。

### マクロ試験

```powershell
& '.\Emuera以外(開発補助ツールなど)\マクロ性能テスト.ps1' -ExePath '.\配布\再発行確認\Emuera.exe' -RepeatCount 10 -Iterations 1 -TimeoutSeconds 180 -CaptureScreenshots
```

N10で画面と操作を確認してから、N100、N1000、最後にN5000へ進む。最初からN5000を流さない。

### 内部計測版

```powershell
dotnet publish .\Emuera.csproj -c Release -r win-x64 --self-contained false -p:PublishSingleFile=true -p:EnablePerformanceMetrics=true -o .\作業用\計測版
```

通常配布へ `EnablePerformanceMetrics=true` は使わない。`EnableStartupMemoryTrim` はプロジェクト既定値trueで、診断上だけ明示falseにして無効化できる。

## 6. 薄い作業ログ

| 時期 | 内容 | 結論 |
|---|---|---|
| 2026-07-14 | 旧系統EXEではゲーム固有関数を解釈できず起動失敗 | `BugFix_Test` を正式な基礎に決定 |
| 2026-07-15 未明 | 起動時だけ不定期に出るラベル・関数警告を調査 | 並列解析の共有状態競合を修正 |
| 2026-07-15 未明 | マウスサイドボタンをページ選択へ接続 | 文言一致方式を採用。ゲームERB変更なし |
| 2026-07-15 朝 | 起動区間とマクロ区間の計測機構・自動試験を作成 | 計測版だけ有効、通常版は無効 |
| 2026-07-15 朝 | マクロの画面更新を最大30 FPSへ集約 | ゲーム処理・表示データ生成・UIイベント処理は維持 |
| 2026-07-15 午前 | 起動並列化、Rename高速経路、初期描画抑制を検証 | 改善したものだけ採用 |
| 2026-07-15 昼 | ERBキャッシュと旧方式の起動時メモリ圧縮を検証 | キャッシュは遅く、旧方式は通常版不採用。現在は2 GiB threshold付きCandidate Kを採用 |
| 2026-07-15 午後 | 17～29番のホットパス候補を個別検証 | 総時間が改善しないため全て撤回 |
| 2026-07-15 最終 | 30番相当の安定ソースを再発行・再試験 | 最終通常版に確定 |
| 2026-07-15 夕方 | サイドボタン候補を追加 | 調教1007/1009、ショップPREV/NEXTを既定対応。設定JSONも可読化 |
| 2026-07-15 夜 | ショップ成功・調教失敗の手動結果を反映 | HTMLボタンの表示文だけでなく内部入力値との完全一致判定を追加。設定JSONの文字化けも回避 |
| 2026-07-15 夜 | 調教画面の診断ログを取得 | 入れ子HTML内の1007/1009が通常の直下検索から見えていないと判明。再帰検索で実画面動作を確認 |
| 2026-07-15 夜 | N1000をCPU・割り当てプロファイル | `MATCH` / `CMATCH` の添字確定済み参照と小配列の使い捨てが主要因と判明 |
| 2026-07-15 夜 | `MATCH` / `CMATCH` の一時参照を貸出式で再利用 | N1000で約7.54%短縮（ERB実行回数補正後）、GC回数も減少 |
| 2026-07-15 夜 | 同じ安全策を `VARSET` / `CVARSET` へ限定拡張 | 最終候補は今回の変更前よりN1000約14.12%短縮。通常版へ採用 |
| 2026-07-15 深夜 | 現行最終版をCPUプロファイル | 描画ではなく、単一埋め込みを含む整形文字列生成が残るホットパスと判明 |
| 2026-07-15 深夜 | 単一埋め込み整形文字列を直接連結 | N1000約3.18%短縮、N5000完走。通常版へ `MACRO-06` として採用 |
| 2026-07-15 深夜 | 汎用文字列バッファの容量見積りも試作 | N100反復中にプロセス終了が発生したため即時撤回。現行ソースに残さず |
| 2026-07-31 | CRER氏の`ClipBoardDialog`高速化を取り込み | 全ログを維持したまま設定中の再描画を抑制。Ctrl+C・メニューとも手動確認済み |

## 7. 重要な判断

- 起動キャッシュは「プレイヤーが消す必要のない安全な自動無効化」を前提に試したが、速度改善がないため採用しなかった
- 小規模構成は速度を優先する。大規模構成では起動完了時managed memoryが2 GiB以上の場合だけ、約1秒程度のtradeoffでWorking Setを大幅削減する
- マクロ高速化は実処理省略ではなく描画集約を優先した
- ERBホットパスは、評価順を変える式変形ではなく、処理中だけ専有する一時オブジェクト再利用を採用した
- 一時参照プールは整数・文字列を分離し、スレッドごとに最大64個。再帰中の貸出物は再利用しない
- 単一埋め込み文字列は式を同じ位置で1回評価し、結果と前後の固定文字だけを連結する。複数埋め込みは従来経路のまま
- これ以上の文字列式木の平坦化や関数引数の再利用は互換性リスクが上がるため、明確なプロファイル根拠なしには進めない
- 画面外行の遅延処理などは互換性リスクが高いため、プロファイルで明確な根拠が出るまで保留
- ランダム戦闘で実行量が揺れる。単発の数値だけで採否を決めず、同一条件の複数回と内部区間を確認する
- ログ画面の高速化はログ生成を省かず、表示欄へ設定中の途中再描画だけを止める

## 8. 既知の制約

- サイドボタンは表示文の部分一致または内部入力値の完全一致で探すため、どちらも候補に合わない画面では動作しない
- `setting_user.json` 保存時にサイドボタン用の2項目が追加される
- N5000はこのゲームでは約5分前後かかり、戦闘内容で変動する
- ランダムシードを固定していないため、別起動間の状態ハッシュ完全一致は保証試験に使えない
- 長時間の `GC.GetTotalAllocatedBytes(false)` は高速な概算値の更新境界で大きく揺れるため、割り当て比較は複数回のN100とGC回数を優先する
- 最終EXEはframework-dependentなので、未導入PCには.NET 10 Desktop Runtime x64が必要

## 9. 参照順

作業再開時は次の順で読む。

1. `04_編集ルール.md`
2. 本書
3. `01_仕様書.md`
4. `03_資産目録.md`
5. `05_採用済み変更ファイル一覧.md`
6. `06_コード案内.md`
7. 必要な調査履歴・検証要約だけ
